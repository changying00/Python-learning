### 元组 tuple :

```python
它的底层采用 线性表 进行数据存储、它存储的元素 有序且可重复的、元组是不可变的

元组 在定义的时候 使用() 语法定义、且性能 比列表更高

元组的 定义方式
  a) 字面量
  b) tuple 工厂函数
     
     tuple 工厂函数 可以将任意的 可迭代对象 转成元组
  # 创建一个长度为1 的元组、此时 必须 添加一个逗号
  tp2  =(1,)
  
  # 在使用 字面量 定义 元组的时候 如果 元组的数据个数 大于0、还可以省略 小括号
  # 不建议省略小括号
  tp3 = 10,20
  
  a = 10
  b = 20
  
  #实现 2个数字的交换
  # b ,a 俩个变量的值 构成了 一个元组 、采用 解包的方式 将元组中的数据分别 赋值给 a,b
  
  a,b =b,a  #其实后面是 b,a 是元组
  
# python 中也存在 元组生成推导式，但是生成的不是元组，需要tuple(),转化一下

```

### 元组支持的方法

	元组支持的方法

```
  count(val):通知 val值 在元组中的个数
  index(val，start，end):获取val 第一次在元组中出现的索引位置
  如果 传入 start ~ stop 代表 在 指定区间 统计 或者 查找

    start 如果 不传、 默认为 0 
    end 如果不传， 默认 为 可迭代对象的长度 
```

### 元组支持的运算：

```
  算术运算符:
  关系运算符:
  成员运算符:
  
  
```

### 元组 支持的遍历方式  

```
a) 基于 索引的遍历 、配合 len 函数 

b) 基于 值的 遍历 

c) 基于 索引和值的遍历、 配合 enumerate 函数 
```

### 元组可以切片操作 

切片 :  是 元组 中 快速提取 指定区间数据的 是一种 技术 手段

  提取 数据 

  [start:stop:step]

列表和元组可以相互转变,

```python
lis = ["DGX","19","hx"]
print(tuple(lis) ) #把列表转换为元组
first = ("mubapen","meixi","neimaer")
print(list(first))#把元组转换为列表
```

### 总结

```

列表和元组都是容器型的数据类型，即一个变量可以保存多个数据，
而且它们都是按一定顺序组织元素的有序容器。列表是可变数据类型，
元组是不可变数据类型，所以列表可以添加元素、删除元素、清空元素、排序反转，
但这些操作对元组来说是不成立的。列表和元组都可以支持拼接运算、成员运算、索引运算、切片运算等
操作，后面我们要讲到的字符串类型也支持这些运算，因为字符串就是字符按一定顺序构成的序列，
在这一点上三者并没有什么区别。我们推荐大家使用列表的生成式语法来创建列表，
它不仅好用而且效率很高，是 Python 语言中非常有特色的语法。

```

### 元组支持的运算 :

```
算术运算符 :  
    + 
    * 

关系运算符 :  >,  >=,  <,  <=,   == ,  !=

成员运算符 :   
    in  /  not in 
```

<br/>

<br/>

<br/>

### 集合 set

```
是一个用来 存储多个值的容器、底层用 散列表+ (再hash法)链表结构 进行数据存储

特点：存储的数据是无序的、且不可重复的(会去重)
```

### 集合 set 的创建方法 

```
a) 字面量 

b) set 工厂函数 
    可以 将 任意 可迭代对象 转成集合 

c) 集合生成推导式  

    语法 {exp  for var in iterable [for var in iterable ...] [if condition]}
```

sorted 是 一个内置函数、可以对任意的可迭代对象进行排序 

```
sorted(iterable, *, reverse=False, key=None)


已知 一个列表 、要求 对 列表中的元素 去重 、并返回一个新列表、且元素 保留原有的顺序。
使用 set 工厂函数 
ls = [3, 65, 87, 123, 65, 1, 3]
将 列表转成 集合 
s4 = set(ls)
print(s4, type(s4)) # 去重后 保持 原有顺序 
print(sorted(s4, key=lambda d: ls.index(d)))

```