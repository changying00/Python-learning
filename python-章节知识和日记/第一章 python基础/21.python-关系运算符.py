"""
关系运算符: 用来比较 数据的大小、 表达式 返回的结果是 一个 bool 值 

 > (大于),  >=  (大于等于) ,  <  (小于),  <= (小于等于),  == (等于) ,  !=  (不等于)
 
作用：
    为 后续的 循环、判断 打基础 

"""

# 定义 2个数字 
a, b = 3, 5

# 判断 a 是否大于 b 
c = a <= b

# 判断 a 是否 大于等于 1 小于等于 b 

d = 1 <= a <= b

print(c, d)

