#将一个二维列表转换成一维列表，例如[[1],[2,3],[4]]→ [1, 2, 3, 4]。
ls1 = [[1],[2,3,3,32,32,44],[4]]
#定义一个空列表接收数据
ls2 = []
#for循环遍历ls1 里面的列表个数，有三个
for i in range(len(ls1)):
    #for循环遍历ls1[i],里面的元素个数
    for j in range(len(ls1[i])):
        #把每个个数拿出来，加到ls2中
        ls2.append(ls1[i][j])
#循环结束，打印数据
print(ls2)