1. 

遇到随机排序，不要自己随机生成不重复数字，优先想到 random.shuffle()。

2. 

大量重复数据不要手写，用循环生成。

3. 

写代码前先设计数据结构：数据怎么存，比代码怎么写更重要。

4. 

循环里面创建变量，会导致数据重置；需要保存的数据放循环外。

5. 

Python切片要熟练：

列表[开始:结束:步长]

|需求|方法|
|--|--|
|随机整数|`random.randint()`|
|随机取一个元素|`random.choice()`|
|随机取多个元素|`random.sample()`|
|随机打乱列表|`random.shuffle()`|
