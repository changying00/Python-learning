================================================================================
《Learning Python》第 6 版 · 全书章节测验 + 中文翻译 + 理解 / 掌握指南
来源：leaning python 6th books/txt/ch01.txt … ch41.txt
提取：各章 “Test Your Knowledge: Quiz” 与 “Test Your Knowledge: Answers”
================================================================================

【使用方法】
1. 先做题（可先看中文题），再对英文答案与中文要点。
2. 每章先读「理解与总结 / 对你的帮助 / 建议掌握程度」。
3. 核心章（类型、动态类型、函数作用域与参数、模块、类、异常）优先刷到能复述。

【总体掌握路线】
  Part I   入门           建议 85–95%
  Part II  类型与运算     建议 90–100%   ← 地基
  Part III 语句与语法     建议 90–95%
  Part IV  函数与生成器   建议 90–100%   ← 分水岭
  Part V   模块与包       建议 80–95%
  Part VI  类与 OOP       建议 85–95%
  Part VII 异常与工具     建议 85–95%
  Part VIII 高级主题      建议 40–75%   ← 按需

【目标：能写项目 / 求职】
  必通测验相关章：4–14, 16–18, 20, 22–24, 26–27, 33–35, 37
  必抠透：第6（引用）、17（作用域）、18（参数）、20（推导/生成器）
  可后置：38 描述符细节、39 复杂装饰器编码题、40 元类
  量化：约 25 个核心章能脱稿讲清答案 ≈ 实用 Python 85%+

【目标：读完教材 + 进阶】
  28–32、36–39 也做；40 懂思想即可；41 无测验

说明：中文答案为忠实浓缩译文，细节以英文原文为准；第39章编码题官方解在书 examples/_QuizAnswers。


========================================================================
第 1 章  A Python Q&A Session
         Python 问答入门
归属：Part I  入门 Getting Started
级别：必读·轻松    优先级：中
========================================================================

【我的理解与总结】
用问答建立 Python 全景：为何用、谁在用、能做什么、何时不用。

【对你的帮助】
建立学习动机与合理预期，避免盲目跟风。

【建议掌握程度】
掌握约 70%：能说出主要理由、应用领域与性能短板。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What are the six main reasons that people choose to use Python?
Q1 [中文] 人们选择使用 Python 的六个主要理由是什么？
A1 [EN] Software quality, developer productivity, program portability, support libraries, component integration, and simple enjoyment. Of these, the quality and productivity themes seem to be the main reasons that people choose to use Python, but enjoyment counts, too, in a field that can be as challenging as software.
A1 [中文] 软件质量、开发者生产力、程序可移植性、支持库、组件集成、以及简单的乐趣。质量与生产力通常是主因。

── 第 2 题 ──
Q2 [EN] Name four notable companies or organizations using Python today.
Q2 [中文] 举出四个今天使用 Python 的知名公司或组织。
A2 [EN] Google, Industrial Light & Magic, JPL, ESRI, Instagram, and many more. Almost every organization doing software development uses Python in some fashion, whether for long-term strategic product development or for short-term tactical tasks such as testing and system administration.
A2 [中文] Google、ILM、JPL、ESRI、Instagram 等。几乎所有软件组织都会在某种程度上用 Python。

── 第 3 题 ──
Q3 [EN] Why might you not want to use Python in an application?
Q3 [中文] 为何可能不想在某应用中使用 Python？
A3 [EN] Python’s main downside is performance: in its currently most-common version, at least, it won’t run as quickly as fully compiled languages like C and C++. On the other hand, it’s quick enough for most applications, and typical Python code runs at close to C speed anyhow because it invokes linked-in C code in the interpreter. If speed is critical, compiled extensions and other tools like Cython are available to optimize the number-crunching parts of a Python application.
A3 [中文] 主要短板是性能：常见实现下不如 C/C++ 快。多数应用够用；热点可用 Cython 等加速。

── 第 4 题 ──
Q4 [EN] What can you do with Python?
Q4 [中文] 你能用 Python 做什么？
A4 [EN] You can use Python for nearly anything you can do with a computer, from website development and gaming to AI and spacecraft control. Numeric programming and web development may lead the pack today, though probably because those are some of the main things for which computers are used.
A4 [中文] 几乎所有能用计算机做的事：Web、游戏、AI、航天控制等。

========================================================================
第 2 章  How Python Runs Programs
         Python 如何运行程序
归属：Part I  入门 Getting Started
级别：必读·基础    优先级：高
========================================================================

【我的理解与总结】
源码→字节码→PVM→机器码；CPython 等实现差异。

【对你的帮助】
理解运行与性能、.pyc、面试常考点。

【建议掌握程度】
掌握约 90%：五条执行链术语与 CPython 地位。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What is the Python interpreter?
Q1 [中文] 什么是 Python 解释器？
A1 [EN] The Python interpreter is a program that runs the Python programs you write. It essentially intermediates between your Python instructions and those available in a CPU’s machine code.
A1 [中文] 运行你所写 Python 程序的程序，在 Python 指令与 CPU 机器码之间中介。

── 第 2 题 ──
Q2 [EN] What is source code?
Q2 [中文] 什么是源代码？
A2 [EN] Source code is the statements you write for your program. It consists of text in text files whose names normally end with a.py extension.
A2 [中文] 你编写的程序语句，通常是 .py 文本文件。

── 第 3 题 ──
Q3 [EN] What is bytecode?
Q3 [中文] 什么是字节码？
A3 [EN] Bytecode is the lower-level form of your program after Python compiles it. Python automatically stores bytecode in files with a.pyc extension when possible, and automatically re-creates it when needed.
A3 [中文] 编译后的较低级形式，可存为 .pyc，需要时自动重建。

── 第 4 题 ──
Q4 [EN] What is the PVM?
Q4 [中文] 什么是 PVM？
A4 [EN] The PVM is the Python Virtual Machine—the runtime engine of Python that interprets your compiled bytecode.
A4 [中文] Python 虚拟机：解释字节码的运行时引擎。

── 第 5 题 ──
Q5 [EN] What is machine code?
Q5 [中文] 什么是机器码？
A5 [EN] Machine code is the low-level instructions of the underlying CPU on a computing device like a PC or phone. Because this is what every program ultimately runs, Python code must be translated to this by a software layer like the PVM interpreter, or JIT or AOT compilers.
A5 [中文] 底层 CPU 指令；一切最终都落到机器码。

── 第 6 题 ──
Q6 [EN] Name two or more variations on Python’s standard execution model.
Q6 [中文] 举出两种以上对标准执行模型的变体。
A6 [EN] Numba, Shed Skin, and standalone executables are all variations on the execution model. In addition, the alternative implementations of Python named in the next two answers modify the model in some fashion as well—by replacing bytecode and VMs, or by adding JIT and AOT compilers.
A6 [中文] Numba、Shed Skin、独立可执行文件；以及替代实现（换 VM/加 JIT 等）。

── 第 7 题 ──
Q7 [EN] How are CPython, Jython, and IronPython different?
Q7 [中文] CPython、Jython、IronPython 有何不同？
A7 [EN] CPython is the standard and reference implementation of the language. Jython and IronPython process Python programs for use in Java and.NET environments, respectively; they are alternative compilers for Python.
A7 [中文] CPython 是标准参考实现；Jython/IronPython 分别面向 Java 与 .NET。

── 第 8 题 ──
Q8 [EN] What are PyPy, Shed Skin, and Cython?
Q8 [中文] PyPy、Shed Skin、Cython 是什么？
A8 [EN] PyPy and Shed Skin are reimplementations of Python targeted at speed. PyPy speeds normal Python programs by using runtime type information and a JIT compiler to replace some Python bytecode with machine code as the program runs. Shed Skin speeds programs with an AOT compiler that translates a restricted subset of Python to C++, from which it can be fully compiled to machine code to be run as a program or used in others. Cython is a Python/C combination that can be compiled into machine-code extensions accessible to CPython code.
A8 [中文] PyPy：JIT 加速；Shed Skin：受限 Python→C++ AOT；Cython：Python/C 混合扩展。

========================================================================
第 3 章  How You Run Programs
         你如何运行程序
归属：Part I  入门 Getting Started
级别：必读·基础    优先级：高
========================================================================

【我的理解与总结】
REPL、命令行、IDLE、import/reload、模块命名空间。

【对你的帮助】
真正会“跑起来”，区分 $ 与 >>>。

【建议掌握程度】
掌握约 95%：独立运行脚本；理解 import 一次。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] How can you start a Python interactive interpreter session (REPL)?
Q1 [中文] 如何启动交互式解释器（REPL）？
A1 [EN] A Python interactive session can be started by typing a Python command line in your system’s console window. Type py on Windows and python3 everywhere else and type it into a Command Prompt or PowerShell window on Windows, Terminal on macOS and Linux, and the Termux app on Android. Another alternative is to launch IDLE, as its main Shell window is an interactive session. Other IDEs, smartphone apps, and browser-based systems may offer REPLs in more unique ways.
A1 [中文] 控制台输入 py（Windows）或 python3；或用 IDLE Shell 等。

── 第 2 题 ──
Q2 [EN] Where do you type a system command line to launch a script file?
Q2 [中文] 在哪里输入系统命令行启动脚本？
A2 [EN] You type system command lines in the same interface used to launch an interactive session by command line. This is whatever your platform provides as a system console: again, Command Prompt or PowerShell on Windows, Terminal on macOS and Linux, Termux on Android, or other. You type this at the system’s prompt shown as $ in this book, not at the Python interactive interpreter’s >>> prompt used to enter Python code—be careful not to confuse these prompts, because Python and the system shell are different systems.
A2 [中文] 系统控制台（cmd/PowerShell/Terminal），是 $ 提示符不是 >>>。

── 第 3 题 ──
Q3 [EN] Name four or more ways to run the code saved in a script file.
Q3 [中文] 举出四种以上运行脚本的方式。
A3 [EN] Code in a script (really, module) file can be run with system command lines, file icon clicks, imports and reloads, the exec built-in function, os module tools, and IDE GUI devices such as IDLE’s Run → Run Module menu option. Some platforms support more specialized launching techniques, like drag and drop on macOS, app UIs on smartphones, and web notebooks. In addition, some text editors have unique ways to run Python code, some Python programs are provided and run as standalone executables, and some systems use Python code in embedded mode, where it is run by an enclosing program written in another language. Though in its early days, code may also be run in web browsers with a Python port to WebAssembly like Pyodide.
A3 [中文] 命令行、图标、import/reload、exec、os 工具、IDLE Run Module 等。

── 第 4 题 ──
Q4 [EN] What pitfall is related to clicking file icons on Windows and Linux?
Q4 [中文] 点击文件图标有何陷阱？
A4 [EN] Scripts that print and then exit cause the output file to disappear immediately, before you can view the output. input() can pause before exit so the output window stays open, but error messages generated by your script also appear in an output window that closes before you can examine its contents—and before an input() pause is reached. Hence, system command lines and IDEs such as IDLE are better for most development.
A4 [中文] 窗口可能立刻关闭看不到输出/错误；开发更宜命令行或 IDLE。

── 第 5 题 ──
Q5 [EN] Why might you need to reload a module?
Q5 [中文] 为何可能需要 reload？
A5 [EN] Python imports (i.e., loads) a module only once per process, by default, so if you’ve changed its source code and want to run the new version without stopping and restarting Python, you’ll have to reload it. You must import a module at least once before you can reload it. Running files of code from a system shell command line, via an icon click, or in an IDE such as IDLE generally makes this a nonissue, as those launch schemes usually run the current version of the source code file each time. An exec / open pair can avoid reloads too.
A5 [中文] 默认每进程只 import 一次；改源码不重启则需 reload。

── 第 6 题 ──
Q6 [EN] How do you run a script from within the IDLE GUI?
Q6 [中文] 如何在 IDLE 中运行脚本？
A6 [EN] Within the text edit window of the file you wish to run, select the window’s Run → Run Module menu option. This runs the window’s source code as a top-level script file in IDLE and displays its output back in the interactive Python “Shell” window.
A6 [中文] 编辑窗口 Run → Run Module。

── 第 7 题 ──
Q7 [EN] How are modules, attributes, and namespaces related?
Q7 [中文] 模块、属性、命名空间关系？
A7 [EN] Each module file is automatically a namespace—that is, a package of variables reflecting the assignments made at the top level of the file. Each of the module’s variables becomes an attribute of the module when it’s imported and are accessed by “.” qualification or from name copies. Namespaces help avoid name collisions in Python programs: because each module file is a self-contained namespace, files must explicitly import other files in order to use their names.
A7 [中文] 每个模块是命名空间；顶层名字成为导入后属性，减少冲突。

========================================================================
第 4 章  Introducing Python Object Types
         Python 对象类型入门
归属：Part II 类型与运算 Types and Operations
级别：必读·核心    优先级：极高
========================================================================

【我的理解与总结】
核心类型地图：不可变/序列/映射/多态。

【对你的帮助】
全书类型体系导航图。

【建议掌握程度】
掌握约 95%：分类类型并解释四个关键词。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] Name four of Python’s core object types.
Q1 [中文] 举出四种核心对象类型。
A1 [EN] Numbers, strings, lists, dictionaries, tuples, files, and sets are generally considered to be the core object types. Booleans, None, and types themselves are classified this way as well. Some of these types are really categories: there are multiple number types (integer, floating point, complex, fraction, and decimal) and multiple string types (text strings, byte strings, and mutable byte strings).
A1 [中文] 数字、字符串、列表、字典、元组、文件、集合等；布尔、None、类型本身也可算。

── 第 2 题 ──
Q2 [EN] Why are they called “core” object types?
Q2 [中文] 为何叫“核心”类型？
A2 [EN] They are known as “core” object types because they are part of the Python language itself and are always available. To create other objects, you generally must call functions in imported modules. Most of the core objects have specific syntax for generating their objects: 'hack', for example, is an expression that makes a string and determines the set of operations that can be applied to it. Because of this, core objects are hardwired into Python’s syntax. In contrast, you must call the built-in open function to create a file object, even though this is usually considered a core object type too.
A2 [中文] 语言内置、始终可用，多数有字面量语法。

── 第 3 题 ──
Q3 [EN] What does “immutable” mean, and which three of Python’s object types are considered immutable?
Q3 [中文] “不可变”含义？哪三种不可变？
A3 [EN] An “immutable” object is an object that cannot be changed after it is created. Numbers, strings, and tuples in Python fall into this category. While you cannot change an immutable object in place, you can always make a new one by running an expression. bytearrays offer mutability for strings, but they only apply directly to text if it’s a simple 8-bit kind (e.g., ASCII).
A3 [中文] 创建后不能原地改。数字、字符串、元组。

── 第 4 题 ──
Q4 [EN] What does “sequence” mean, which objects fall into this category, and how is “iterable” related?
Q4 [中文] “序列”含义？与可迭代关系？
A4 [EN] A “sequence” is a positionally ordered collection of objects. Strings, lists, and tuples are all sequences in Python. They share common sequence operations, such as indexing, concatenation, and slicing, but also have type-specific method calls. The related term “iterable” means either a physical sequence, or a virtual one that produces its items on request. Sequences are iterable, but so are generators, files, results of functions like range, and the dictionary object (which produces its keys when iterated, just like its keys method).
A4 [中文] 有序集合：str/list/tuple。可迭代含序列及生成器、文件、range、dict 键等。

── 第 5 题 ──
Q5 [EN] What does “mapping” mean, and which core object type is a mapping?
Q5 [中文] “映射”含义？哪个是映射？
A5 [EN] The term “mapping” denotes an object that maps keys to associated values. Python’s dictionary is the only mapping among its core object types. Mappings retain insertion order (as of Python 3.7), and support access to data stored by key, plus type-specific method calls that enable key tests, iteration, and more.
A5 [中文] 键→值。核心类型中字典是映射。

── 第 6 题 ──
Q6 [EN] What is “polymorphism,” and why should you care?
Q6 [中文] 什么是多态？为何重要？
A6 [EN] “Polymorphism” means that the meaning of an operation (like a +) depends on the objects being operated on. This turns out to be a key idea (and perhaps the largest) behind using Python well—not constraining code to specific types makes that code automatically applicable to many types. This becomes more obvious when coding functions and classes in Python. While Python today has type hinting, it’s not used by Python itself and is meant only for documentation and tools.
A6 [中文] 运算含义取决于对象类型；不绑死类型才能通用——用好 Python 的关键。

========================================================================
第 5 章  Numeric Types
         数字类型
归属：Part II 类型与运算 Types and Operations
级别：必读·核心    优先级：高
========================================================================

【我的理解与总结】
数字运算、优先级、类型提升、进制。

【对你的帮助】
写对表达式、处理进制与精度。

【建议掌握程度】
掌握约 90%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What is the value of the expression 2 * (3 + 4) in Python, and why?
Q1 [中文] 2*(3+4) 的值？为何？
A1 [EN] The value will be 14, the result of 2 * 7, because the parentheses force the addition to happen before the multiplication.
A1 [中文] 14，括号使加法先算。

── 第 2 题 ──
Q2 [EN] What is the value of the expression 2 * 3 + 4 in Python, and why?
Q2 [中文] 2*3+4 的值？为何？
A2 [EN] The value will be 10, the result of 6 + 4. Python’s operator precedence rules are applied in the absence of parentheses, and multiplication has higher precedence than (i.e., happens before) addition, per Table 5-2.
A2 [中文] 10，乘法优先于加法。

── 第 3 题 ──
Q3 [EN] What is the value of the expression 2 + 3 * 4 in Python, and why?
Q3 [中文] 2+3*4 的值？为何？
A3 [EN] This expression yields 14, the result of 2 + 12, for the same precedence reasons as in the prior question.
A3 [中文] 14，乘法优先。

── 第 4 题 ──
Q4 [EN] What tools can you use to find a number’s square root, as well as its square?
Q4 [中文] 如何求平方根与平方？
A4 [EN] Functions for obtaining the square root, as well as pi, tangents, and more, are available in the imported math module. To find a number’s square root, import math and call math.sqrt(N). To get a number’s square, use either the exponent expression X ** 2 or the built-in function pow(X, 2). Either of these last two can also compute the square root when given a power of 0.5 (e.g., X **.5).
A4 [中文] math.sqrt；X**2 或 pow(X,2)；X**0.5 也可开方。

── 第 5 题 ──
Q5 [EN] What is the type of the result of the expression 1 + 2.0 + 3, and why?
Q5 [中文] 1+2.0+3 结果类型？
A5 [EN] The result will be a floating-point number: the integers are converted up to floating point, the most complex type in the expression, and floating- point math is used to evaluate it.
A5 [中文] 浮点：向最复杂的浮点类型提升。

── 第 6 题 ──
Q6 [EN] How can you truncate and round a floating-point number?
Q6 [中文] 如何截断与四舍五入？
A6 [EN] The int(N) and math.trunc(N) functions truncate, and the round(N, digits) function rounds. We can also compute the floor with math.floor(N) and round for display with string-formatting operations.
A6 [中文] int/trunc 截断；round 四舍五入；floor 向下。

── 第 7 题 ──
Q7 [EN] How can you convert an integer to a floating-point number?
Q7 [中文] 整数如何变浮点？
A7 [EN] The float(I) function converts an integer to a floating point; mixing an integer with a floating point within an expression will result in a conversion as well. In some sense, Python / true division converts too —it always returns a floating-point result that includes the remainder, even if both operands are integers.
A7 [中文] float(I) 或与浮点混合；/ 真除法返回浮点。

── 第 8 题 ──
Q8 [EN] How would you display an integer in octal, hexadecimal, or binary notation?
Q8 [中文] 如何显示八/十六/二进制？
A8 [EN] The oct(I), hex(I), and bin(I) built-in functions return the octal, hexadecimal, and binary string forms for an integer. All three flavors of string formatting (expression, method, and f-string) also provide targets for some such conversions.
A8 [中文] oct/hex/bin；或字符串格式化。

── 第 9 题 ──
Q9 [EN] How might you convert an octal, hexadecimal, or binary string to a plain integer?
Q9 [中文] 八/十六/二进制字符串如何变整数？
A9 [EN] The int(S, base) function can be used to convert from octal, hexadecimal, and binary digit strings to normal integers (pass in 8, 16, or 2 for the base). The eval(S) function can be used for this purpose too, but it’s more expensive to run and can have security risks. To some extent, other-base literals like 0xFFFF and 0b1111 in your code do this work too when read by Python. Note that integers are always stored in binary form in computer memory; these are just display string format conversions.
A9 [中文] int(S, base)；eval 不推荐。

========================================================================
第 6 章  The Dynamic Typing Interlude
         动态类型插曲
归属：Part II 类型与运算 Types and Operations
级别：必读·核心    优先级：极高
========================================================================

【我的理解与总结】
名字与对象、共享引用、可变/不可变副作用。

【对你的帮助】
Python 最重要心智模型，后面参数/默认值全靠它。

【建议掌握程度】
掌握约 100%：能画引用图判断是否互相影响。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] Consider the following three statements. Do they change the value printed for A ? A = 'code' B = A B = 'Python'
Q1 [中文] A='code'; B=A; B='Python' 会改变 A 吗？
A1 [EN] No: A still prints as 'code'. When B is assigned to the string 'Python', all that happens is that the variable B is reset to point to the new string object. A and B initially share (i.e., reference/point to) the same single string object 'code', but two names are never linked together in Python. Thus, setting B to a different object has no effect on A. The same would be true if the last statement here were B = B + 'coding', by the way—the concatenation would make a new object for its result, which would then be assigned to B only. We can never overwrite a string (or number, or tuple) in place, because strings are immutable.
A1 [中文] 否。B 改指向新串；名字不互相绑定。

── 第 2 题 ──
Q2 [EN] Consider these three statements. Do they change the printed value of A ? A = ['code'] B = A B[0] = 'Python'
Q2 [中文] A=['code']; B=A; B[0]='Python' 会改变 A 吗？
A2 [EN] Yes: A now prints as ['Python']. Technically, we haven’t really changed either A or B; instead, we’ve changed part of the object they both reference (point to) by overwriting that object in place through the variable B. Because A references the same object as B, the update is reflected in A, too.
A2 [中文] 会。二者共享同一列表，原地修改可见。

── 第 3 题 ──
Q3 [EN] How about these—is A changed now? A = ['code'] B = A[:] B[0] = 'Python'
Q3 [中文] A=['code']; B=A[:]; B[0]='Python' 会改变 A 吗？
A3 [EN] No: A still prints as ['code']. The in-place assignment through B has no effect this time because the slice expression made a copy of the list object before it was assigned to B. After the second assignment statement, there are two different list objects that have the same value— in Python, we say they are ==, but not is. The third statement changes the value of the list object pointed to by B, but not that pointed to by A.
A3 [中文] 否。切片浅拷贝，== 但不是 is。

========================================================================
第 7 章  String Fundamentals
         字符串基础
归属：Part II 类型与运算 Types and Operations
级别：必读·核心    优先级：极高
========================================================================

【我的理解与总结】
字符串不可变、方法vs表达式、格式化、转义。

【对你的帮助】
文本处理与 f-string 日常刚需。

【建议掌握程度】
掌握约 95%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] Can the string find method be used to search a list?
Q1 [中文] 字符串 find 能搜列表吗？
A1 [EN] No, because methods are always type specific; that is, they only work on a single object type. Expressions like X + Y and built-in functions like len(X) are generic, though, and may work on a variety of types. In this case, for instance, the in membership expression has a similar effect as the string find, but it can be used to search both strings and lists. Python makes some attempt to name similar methods consistently (many objects have a copy method, for example, and mutable objects may share method names like pop), but methods are still more type specific than other operation sets.
A1 [中文] 不能。方法类型专用；in 更通用。

── 第 2 题 ──
Q2 [EN] Can a string slice expression be used on a list?
Q2 [中文] 字符串切片能用于列表吗？
A2 [EN] Yes. Unlike methods, expressions are generic and apply to many types. In this case, the slice expression is really a sequence operation—it works on any type of sequence object, including strings, lists, and tuples. The only difference is that when you slice a list, you get back a new list.
A2 [中文] 能。切片是序列运算。

── 第 3 题 ──
Q3 [EN] How would you convert a character to its ASCII integer code? How would you convert the other way, from an integer code to a character?
Q3 [中文] 字符与 ASCII 码如何互转？
A3 [EN] The built-in ord(S) function converts from a one-character string to an integer character code; chr(I) converts from the integer code back to a string. Keep in mind, though, that these integers are only ASCII codes for text whose characters are drawn only from the ASCII character set. In the Unicode model, text strings are really sequences of Unicode code point identifying integers, which may fall outside the 7-bit range of numbers reserved by ASCII (we previewed Unicode in Chapter 4 and will revisit it in Chapter 37).
A3 [中文] ord / chr；Unicode 下是码点。

── 第 4 题 ──
Q4 [EN] How might you go about changing a string in Python?
Q4 [中文] 如何“修改”字符串？
A4 [EN] Strings cannot be changed; they are immutable. However, you can achieve a similar effect by creating a new string—by concatenating, slicing, using a method call like replace, or running formatting operations—and then assigning the result back to the original variable name.
A4 [中文] 不可变；拼接/切片/replace/格式化生成新串再赋值。

── 第 5 题 ──
Q5 [EN] Given a string S with the value 'c,od,e', name two ways to extract the two characters in the middle.
Q5 [中文] S='c,od,e' 如何取中间两字符？
A5 [EN] You can slice the string using S[2:4] or split on the comma and index the string using S.split(',')[1]. Try these interactively to see for yourself.
A5 [中文] S[2:4] 或 S.split(',')[1]。

── 第 6 题 ──
Q6 [EN] How many characters are there in the string "a\nb\x1f\000d" ?
Q6 [中文] "a\nb\x1f\000d" 有多少字符？
A6 [EN] Six. The string "a\nb\x1f\000d" contains the characters a, newline (\n), b, literal value 31 (as hex escape \x1f, which is a code point that stands for the nonprintable control character US), literal value 0 (an octal escape \000), and d. Pass the string to the built-in len function to verify this and print each of its characters’ ord results to see the actual code point (identifying number) values. See Table 7-2 for more details on escapes.
A6 [中文] 六个。

── 第 7 题 ──
Q7 [EN] Write an expression, method call, and f-string to format 'Python' and 3.12 at a string’s beginning and end.
Q7 [中文] 用三种方式格式化 'Python' 与 3.12。
A7 [EN] There’s no right answer for what goes in the middle of the result string, but as examples: '%s is %s' % ('Python', 3.12) works for the expression, and '{} is {}'.format('Python', 3.12) suffices for the method call. There’s almost no reason to use an f-string if all parts are known and formatted per defaults (f'{'Python'} is {3.12}' seems silly), but f-strings are more useful if values are first assigned to variables: x='Python'; y=3.12; f'{x} is {y}' or similar garners full points.
A7 [中文] % 表达式、str.format、f-string（变量更自然）。

========================================================================
第 8 章  Lists and Dictionaries
         列表与字典
归属：Part II 类型与运算 Types and Operations
级别：必读·核心    优先级：极高
========================================================================

【我的理解与总结】
list/dict 构建与原地修改、选型。

【对你的帮助】
几乎每个程序都用。

【建议掌握程度】
掌握约 95%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] Name two ways to build a list containing five integer zeros.
Q1 [中文] 两种方法得到五个 0 的列表。
A1 [EN] A literal expression like [0, 0, 0, 0, 0] and a repetition expression like [0] * 5 will each create a list of five zeros. In practice, you might also build one up with a loop that starts with an empty list and appends 0 to it in each iteration, with L.append(0). A list comprehension like [0 for i in range(5)] could work here, too, but this is more work than you need to do for this answer.
A1 [中文] [0]*5 或 [0,0,0,0,0]；也可循环/推导。

── 第 2 题 ──
Q2 [EN] Name two ways to build a dictionary with two keys, 'a' and 'b', each having an associated value of 0.
Q2 [中文] 两种方法得到 {'a':0,'b':0}。
A2 [EN] A literal expression such as {'a': 0, 'b': 0} or a series of assignments like D = {}, D['a'] = 0, and D['b'] = 0 would create the desired dictionary. You can also use the newer and simpler-to-code dict(a=0, b=0) keyword form, or the more flexible dict([('a', 0), ('b', 0)]) key/value sequences form. Because all the values are the same, you can also use the special form dict.fromkeys('ab', 0), and dictionary comprehension like {k: 0 for k in 'ab'} suffices too, though again, this may be overkill here.
A2 [中文] 字面量、逐步赋值、dict(a=0,b=0)、fromkeys、推导等。

── 第 3 题 ──
Q3 [EN] Name four operations that change a list object in place.
Q3 [中文] 四种原地改列表的操作。
A3 [EN] The append and extend methods grow a list in place, the sort and reverse methods order and reverse lists, the insert method inserts an item at an offset, the remove and pop methods delete from a list by value and by position, the del statement deletes an item or slice, and index and slice assignment statements replace an item or entire section. Pick any four of these for the quiz.
A3 [中文] append/extend、sort/reverse、insert、remove/pop、del、切片赋值等。

── 第 4 题 ──
Q4 [EN] Name four operations that change a dictionary object in place.
Q4 [中文] 四种原地改字典的操作。
A4 [EN] Dictionaries are primarily changed by assignment to a new or existing key, which creates or changes the key’s associated value in the table. Also, the del statement deletes a key’s entry, the dictionary update method merges one dictionary into another in place, and D.pop(key) removes a key and returns the value it had. Dictionaries also have other, more exotic in-place change methods presented tersely or not at all in this chapter, such as popitem and setdefault; see reference resources for more details.
A4 [中文] 键赋值、del、update、pop 等。

── 第 5 题 ──
Q5 [EN] Why might you use a dictionary instead of a list?
Q5 [中文] 为何用字典而非列表？
A5 [EN] This question is a bit unfair, given that the following sidebar gives the answer, but you may have already figured this out on your own. Dictionaries are generally better when the data is labeled (a record with field names, for example); lists are best suited to collections of unlabeled items (such as all the files in a directory). Dictionary lookup is also usually quicker than searching a list, though this might vary per program and Python.
A5 [中文] 带标签的记录更清晰；按键查找通常更快。

========================================================================
第 9 章  Tuples, Files, and Everything Else
         元组、文件与其他
归属：Part II 类型与运算 Types and Operations
级别：必读·核心    优先级：高
========================================================================

【我的理解与总结】
元组、文件、pickle/json、深拷贝、真值。

【对你的帮助】
I/O 与序列化实用技能。

【建议掌握程度】
掌握约 90%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] How can you determine how large a tuple is? Why is this tool located where it is?
Q1 [中文] 如何知元组大小？工具为何在那？
A1 [EN] The built-in len function returns the length (number of contained items) for any container object in Python, including tuples. It is a built-in function instead of a type method because it applies to many different types of objects. In general, built-in functions and expressions may span many object types; methods are specific to a single object type, though some method names may be available on more than one type (index, for example, works on lists and tuples).
A1 [中文] len()；因适用于多种容器，故为内置函数。

── 第 2 题 ──
Q2 [EN] Write an expression that changes the first item in a tuple. (4, 5, 6) should become (1, 5, 6) in the process.
Q2 [中文] 如何把 (4,5,6) 第一项变为 1？
A2 [EN] Because they are immutable, you can’t really change tuples in place, but you can generate a new tuple with the desired value. Given T = (4, 5, 6), you can change the first item by making a new tuple from its parts by slicing and concatenating: T = (1,) + T[1:]. (Recall that single- item tuples require a trailing comma.) You could also convert the tuple to a list, change it in place, and convert it back to a tuple, but this is more expensive and is rarely required in practice—simply use a list if you know that the object will require in-place changes.
A2 [中文] T=(1,)+T[1:]（不可变故造新元组）。

── 第 3 题 ──
Q3 [EN] What is the default for the processing mode argument in a file open call?
Q3 [中文] open 默认模式？
A3 [EN] The default for the processing mode argument in a file open call is 'r', for reading text input. For input text files, simply pass in the external file’s name or path (unless you also need to customize things like buffering policies or provide a Unicode text encoding to override your platform’s default—as fleshed out in Chapter 37).
A3 [中文] 'r' 读文本。

── 第 4 题 ──
Q4 [EN] What module might you use to store Python objects in a file without converting them to strings yourself?
Q4 [中文] 哪个模块可直接存 Python 对象？
A4 [EN] The pickle module can be used to store Python objects in a file without explicitly converting them to strings. json similarly converts a limited set of Python objects to and from strings per the JSON format. The struct module is related, but it assumes the data is to be in packed binary format in the file.
A4 [中文] pickle；json 有限类型；struct 面向二进制打包。

── 第 5 题 ──
Q5 [EN] How might you go about copying all parts of a nested structure at once?
Q5 [中文] 如何深拷贝嵌套结构？
A5 [EN] Import the copy module, and call copy.deepcopy(X) if you need to copy all parts of a nested structure X. This is also rarely needed in practice; references are usually the desired behavior, and shallow copies (e.g., aList[:], aDict.copy(), set(aSet)) usually suffice for most copies.
A5 [中文] copy.deepcopy。

── 第 6 题 ──
Q6 [EN] When does Python consider an object to be true?
Q6 [中文] 对象何时为真？
A6 [EN] An object is considered true if it is either a nonzero number or a nonempty collection object. The built-in words True and False are essentially predefined to have the same meanings as integer 1 and 0, respectively.
A6 [中文] 非零数字或非空集合；True/False≈1/0。

========================================================================
第 10 章  Introducing Python Statements
         Python 语句入门
归属：Part III 语句与语法 Statements and Syntax
级别：必读·核心    优先级：极高
========================================================================

【我的理解与总结】
缩进、冒号、无分号花括号、try 初识。

【对你的帮助】
语法哲学；消灭 IndentationError/忘冒号。

【建议掌握程度】
掌握约 100%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What three things are required in a C-like language but omitted in Python?
Q1 [中文] 类 C 语言要、Python 省略的三样？
A1 [EN] C-like languages require parentheses around the tests in some statements, semicolons at the end of each statement, and braces around a nested block of code. Python requires none of these (but adds a :).
A1 [中文] 条件外括号、句末分号、块花括号（用 : 与缩进）。

── 第 2 题 ──
Q2 [EN] How is a statement normally terminated in Python?
Q2 [中文] 语句通常如何结束？
A2 [EN] The end of a line terminates the statement that appears on that line. Alternatively, if more than one statement appears on the same line, they can be separated with semicolons; similarly, if a statement spans many lines, you must terminate it by closing a bracketed syntactic pair.
A2 [中文] 行尾结束；同行多句用分号；括号跨行至闭合。

── 第 3 题 ──
Q3 [EN] How are the statements in a nested block of code normally associated in Python?
Q3 [中文] 嵌套块如何关联？
A3 [EN] The statements (code lines) in a nested block are all indented the same number of tabs or spaces.
A3 [中文] 相同缩进。

── 第 4 题 ──
Q4 [EN] How can you make a single statement span multiple lines?
Q4 [中文] 单语句如何跨多行？
A4 [EN] You can make a statement span many lines by enclosing part of it in parentheses, square brackets, or curly braces; the statement ends when Python sees a line that contains the closing part of the pair.
A4 [中文] ()[]{} 包裹至闭合。

── 第 5 题 ──
Q5 [EN] How can you code a compound statement on a single line?
Q5 [中文] 复合语句如何写一行？
A5 [EN] The body of a compound statement can be moved to the header line after the colon, but only if the body consists of only noncompound statements.
A5 [中文] 冒号后接非复合体。

── 第 6 题 ──
Q6 [EN] Is there any valid reason to type a semicolon at the end of a statement in Python?
Q6 [中文] 句末分号正当理由？
A6 [EN] Only when you need to squeeze more than one statement onto a single line of code. Even then, this works only if all the statements are noncompound, and it’s discouraged because it can lead to code that is difficult to read.
A6 [中文] 仅挤多条非复合句到一行时；不推荐。

── 第 7 题 ──
Q7 [EN] What is a try statement for?
Q7 [中文] try 做什么？
A7 [EN] The try statement is used to catch and recover from exceptions (errors) in a Python script. It’s often an alternative to manually checking for errors in code.
A7 [中文] 捕获并恢复异常。

── 第 8 题 ──
Q8 [EN] What is the most common coding mistake among Python beginners?
Q8 [中文] 初学者最常见错误？
A8 [EN] Forgetting to type the colon character at the end of the header line in a compound statement is the most common beginner’s mistake. If you’re new to Python and haven’t made it yet, you probably will soon!
A8 [中文] 复合语句头忘记冒号。

========================================================================
第 11 章  Assignments, Expressions, and Prints
         赋值、表达式与打印
归属：Part III 语句与语法 Statements and Syntax
级别：必读·核心    优先级：高
========================================================================

【我的理解与总结】
多种赋值、可变共享危险、sort 返回值、print 重定向。

【对你的帮助】
日常写法与经典坑（L=L.sort()）。

【建议掌握程度】
掌握约 95%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] Name three ways that you can assign three variables to the same value.
Q1 [中文] 三种方式把三个变量赋成同一值。
A1 [EN] You can use multiple-target assignments (A = B = C = 0), sequence assignment (A, B, C = 0, 0, 0), or multiple assignment statements on three separate lines (A = 0, B = 0, and C = 0). With the latter technique, as introduced in Chapter 10, you can also string the three separate statements together on the same line by separating them with semicolons (A = 0; B = 0; C = 0).
A1 [中文] 多目标 A=B=C=0；序列赋值 A,B,C=0,0,0；三行（或分号）分别赋值。

── 第 2 题 ──
Q2 [EN] What’s dangerous about assigning three variables to a mutable object?
Q2 [中文] 三个变量赋成同一可变对象有何危险？
A2 [EN] If you assign them this way: A = B = C = [], then all three names reference the same object, so changing it in place from one (e.g., A.append(99)) will affect the others. This is true only for in-place changes to mutable objects like lists and dictionaries; for immutable objects such as numbers and strings, this issue is irrelevant because they can never be changed in place.
A2 [中文] 共享同一对象，通过一个名字原地修改会反映到所有名字。

── 第 3 题 ──
Q3 [EN] What’s wrong with saying L = L.sort() ?
Q3 [中文] L = L.sort() 有何问题？
A3 [EN] The list sort method is like append in that it makes an in-place change to the subject list—it returns None, not the list it changes. The assignment back to L sets L to None, not to the sorted list. As discussed both earlier and later in this book (e.g., Chapter 8), a newer built-in function, sorted, sorts any sequence and returns a new list with the sorting result; because this is not an in-place change, its result can be safely and meaningfully assigned to a name.
A3 [中文] list.sort 原地排序并返回 None，L 变成 None。应 L.sort() 或 L=sorted(L)。

── 第 4 题 ──
Q4 [EN] How might you use the print operation to send text to an external file?
Q4 [中文] 如何用 print 把文本送到外部文件？
A4 [EN] To print to a file for a single print operation, you can use the print(X, file=F) call form, or assign sys.stdout to a manually opened file before the print and restore the original after if needed. You can also redirect all of a program’s printed text to a file with special syntax in the system shell like > file, but this is outside Python’s scope.
A4 [中文] print(..., file=打开的文件对象) 或重定向 sys.stdout。

========================================================================
第 12 章  if and match Tests and Syntax Rules
         if / match 测试与语法规则
归属：Part III 语句与语法 Statements and Syntax
级别：必读·核心    优先级：高
========================================================================

【我的理解与总结】
多路分支、三元表达式、跨行、True/False、短路。

【对你的帮助】
控制流分支能力。

【建议掌握程度】
掌握约 90%（含 match 了解）。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] How might you code a multiple-choice branch in Python?
Q1 [中文] 如何写多路分支？
A1 [EN] An if statement with multiple elif clauses is often the most straightforward way to code a multiple-choice branch, though not necessarily the most concise or flexible. Dictionary indexing can often achieve the same result, especially if the dictionary contains callable functions coded with def statements or lambda expressions. As of Python 3.10, the match statement provides explicit syntax for multiple- choice selections; it works well in basic roles but can’t code logic as general as that in the if statement and comes with substantial heft in support of structural pattern matching, a very different tool.
A1 [中文] if/elif/else；或字典派发（值可以是函数）；3.10+ 也可用 match/case。

── 第 2 题 ──
Q2 [EN] How can you code an if / else statement as an expression in Python?
Q2 [中文] 如何把 if/else 写成表达式？
A2 [EN] The expression form Y if X else Z returns Y if X is true, or Z otherwise; it’s the same as a four-line if statement and works well in limited contexts, but can’t code actions as rich as those in the full if statement, and has the potential to produce code that’s hard to read. The and / or combination (((X and Y) or Z)) can work the same way, but it’s more obscure and requires that the Y part be true.
A2 [中文] 三元：X if test else Y。

── 第 3 题 ──
Q3 [EN] How can you make a single statement span many lines?
Q3 [中文] 如何让单语句跨多行？
A3 [EN] Wrap up the statement in an open syntactic pair ((), [], or {}), and it can span as many lines as you like; the statement ends when Python sees the closing (right) half of the pair, and lines 2 and beyond of the statement can begin at any indentation level. Backslash continuations work, too, but are broadly discouraged in the Python world.
A3 [中文] 用括号包裹（与第10章相同规则）。

── 第 4 题 ──
Q4 [EN] What do the words True and False mean?
Q4 [中文] True 和 False 是什么意思？
A4 [EN] This is partly a review from Chapter 9, but is reinforced in the sidebar at the end of this chapter. True and False are just custom versions of the integers 1 and 0, respectively: they always stand for Boolean true and false values in Python. They’re available for use in truth tests and variable initialization and are printed for expression results at the interactive prompt. In all these roles, they serve as a more mnemonic and hence readable alternative to 1 and 0.
A4 [中文] 布尔对象，行为上类似 1 和 0，用于真值测试。

── 第 5 题 ──
Q5 [EN] What does “short-circuiting” mean, and where does it crop up?
Q5 [中文] “短路”是什么？出现在哪？
A5 [EN] Short-circuiting happens when Python stops evaluating an expression early because its result can already be determined from the expression so far. It comes up in and and or expressions, which run their right side only if their left sides don’t determine their results. It also comes up in the if / else ternary expression, which runs either its true or false parts, depending on its test part’s logical result.
A5 [中文] and/or 只求值到能决定结果为止；左边已定则不评右边。

========================================================================
第 13 章  while and for Loops
         while 与 for 循环
归属：Part III 语句与语法 Statements and Syntax
级别：必读·核心    优先级：极高
========================================================================

【我的理解与总结】
while/for、break/continue/else、计数循环、range。

【对你的帮助】
循环是算法基础。

【建议掌握程度】
掌握约 95%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What are the main functional differences between while and for loops?
Q1 [中文] while 与 for 主要功能差别？
A1 [EN] The while loop is a general looping statement, but the for is designed to automatically iterate across items in a sequence or other iterable. Although the while can imitate the for with counter loops, it takes more code and might run slower.
A1 [中文] while 通用条件循环；for 自动遍历可迭代对象。while 可模仿 for 但更冗长可能更慢。

── 第 2 题 ──
Q2 [EN] What’s the difference between break and continue ?
Q2 [中文] break 与 continue 差别？
A2 [EN] The break statement exits a loop immediately (control flow winds up below the entire while or for loop statement), and continue jumps back to the top of the loop (control flow winds up positioned just before the test in while or the next item fetch in for).
A2 [中文] break 立刻离开循环；continue 回到循环顶部进入下一轮。

── 第 3 题 ──
Q3 [EN] When is a loop’s else clause executed?
Q3 [中文] 循环 else 何时执行？
A3 [EN] The else clause in a while or for loop will be run once as the loop is exiting, if and only if the loop exits normally (i.e., by a false test in while or an empty object in for), without running into a break statement. A break exits the loop immediately, skipping the else part on the way out (if there is one).
A3 [中文] 循环正常结束（未 break）时。

── 第 4 题 ──
Q4 [EN] How can you code a counter-based loop in Python?
Q4 [中文] 如何写基于计数器的循环？
A4 [EN] Counter loops can be coded with a while statement that keeps track of the index manually, or with a for loop that uses the range built-in function to generate successive integer offsets. Neither is the preferred way to code in Python, if you need to simply step across all the items in a sequence. Instead, use a simple for loop without range or counters, whenever possible; it will be easier to code and usually quicker to run.
A4 [中文] for i in range(N): ... 或 while 自增计数。

── 第 5 题 ──
Q5 [EN] What can a range be used for in a for loop?
Q5 [中文] for 中 range 能做什么？
A5 [EN] The range built-in can be used in a for loop to implement a fixed number of repetitions, to scan by offsets instead of items at offsets, to skip successive items as you go, and to change a list while stepping across it. None of these roles requires range, and most have alternatives —scanning actual items, three-limit slices, and list comprehensions are often better solutions today (despite the natural inclinations of ex–C programmers to want to count things!).
A5 [中文] 产生索引序列、重复 N 次、与 len 配合按位置访问等。

========================================================================
第 14 章  Iterations and Comprehensions
         迭代与推导式
归属：Part III 语句与语法 Statements and Syntax
级别：必读·核心    优先级：高
========================================================================

【我的理解与总结】
迭代协议、推导式、迭代工具、逐行读文件。

【对你的帮助】
为生成器与大数据处理打底。

【建议掌握程度】
掌握约 90%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] How are for loops and iterable objects related?
Q1 [中文] for 与可迭代对象关系？
A1 [EN] The for loop normally uses the iteration protocol to step through items in the iterable object across which it is iterating. It first fetches an iterator from the iterable by calling the iterable’s __iter__, and then calls this iterator object’s __next__ method on each iteration to advance and catches its StopIteration exception to determine when to stop looping. Any object that supports this model works in a for loop and in all other iteration tools. The protocol’s methods can also be run manually with built-ins iter and next. For some objects that are their own iterator, the initial iter call is extraneous but harmless.
A1 [中文] for 用迭代协议：__iter__ 取迭代器，反复 __next__，StopIteration 结束。

── 第 2 题 ──
Q2 [EN] How are for loops and list comprehensions related?
Q2 [中文] for 与列表推导关系？
A2 [EN] Both are iteration tools. List comprehensions are a concise and often efficient way to perform a common for loop task: collecting the results of applying an expression to all items in an iterable object. It’s always possible to translate a list comprehension to a for loop, and part of the list comprehension expression looks like the header of a for loop syntactically. The for loop, however, can be used in additional looping roles that comprehensions do not address.
A2 [中文] 推导是更精炼的“遍历+收集”，常可替换建空表再 append 的 for。

── 第 3 题 ──
Q3 [EN] Name four iteration tools in the Python language.
Q3 [中文] 举出四种迭代工具。
A3 [EN] Iteration tools in Python include the for loop; list comprehensions; the map built-in function; the in membership test expression; and the built- in functions sorted, sum, any, and all. This category also includes the list and tuple built-ins, string join methods, and sequence assignments (starred or not), all of which use the iteration protocol (see answer #1) to step across iterable objects one item at a time.
A3 [中文] for、推导式、map、zip、enumerate、filter、文件迭代等（任举四）。

── 第 4 题 ──
Q4 [EN] What is the best way to read line by line from a text file today?
Q4 [中文] 今天逐行读文本文件的最佳方式？
A4 [EN] The “best” way to read lines from a text file today is to not read it explicitly at all: instead, open the file within an iteration tool such as a for loop or list comprehension, and let the iteration tool automatically scan one line at a time by running the file’s next handler method on each iteration. This approach is generally best in terms of coding simplicity, memory space, and possibly execution speed requirements.
A4 [中文] for line in open(...):  （或 with 打开后同样迭代），不必一次 readlines 全进内存。

========================================================================
第 15 章  The Documentation Interlude
         文档插曲
归属：Part III 语句与语法 Statements and Syntax
级别：必读·工具    优先级：高
========================================================================

【我的理解与总结】
docstring vs #、help/dir、查模块。

【对你的帮助】
自学与读库的外挂。

【建议掌握程度】
掌握约 85%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] When should you use documentation strings instead of hash-mark comments?
Q1 [中文] 何时用文档字符串而非 # 注释？
A1 [EN] Documentation strings (docstrings) are considered best for larger, functional documentation, describing the use of modules, functions, classes, and methods in your code. Hash-mark comments are better limited to smaller-scale documentation about arcane expressions or statements at strategic points on your code. This is partly because docstrings are easier to find in a source file (they have specific locations), but also because they can be extracted and displayed by the Pydoc system.
A1 [中文] 模块/函数/类/方法的功能性大文档用 docstring；# 用于局部难点小注释。

── 第 2 题 ──
Q2 [EN] Name three ways you can view documentation strings.
Q2 [中文] 三种查看文档字符串的方式？
A2 [EN] You can see docstrings by printing an object’s __doc__ attribute, by passing it to Pydoc’s help function, and by selecting modules in Pydoc’s HTML-based browser interface. The latter is launched with a - b command-line switch, and runs a client/server system that displays documentation in a popped-up web browser. Pydoc can also be run to save a module’s documentation in an HTML file for later viewing or printing.
A2 [中文] __doc__、help()、PyDoc/浏览器文档工具等。

── 第 3 题 ──
Q3 [EN] How can you obtain a list of the available attributes in an object?
Q3 [中文] 如何获得对象可用属性列表？
A3 [EN] The built-in dir(X) function returns a list of all the attributes attached to any object. A list comprehension like [a for a in dir(X) if not a.startswith('__')] can be used to filter out internals’ names with underscores (you’ll learn how to wrap this in a function in the next part of the book to make it reusable).
A3 [中文] dir(obj)。

── 第 4 题 ──
Q4 [EN] How can you get a list of all importable modules on your computer?
Q4 [中文] 如何得到本机可导入模块列表？
A4 [EN] Because the index page opened for Pydoc’s browser-based -b mode displays every module on your import search path, this shows all the modules available for imports in your programs. This relies on configurable search-path settings that we’ll cover more fully later in this book, but it includes both the current directory and Python’s standard library by default. You can also write code of your own to achieve this using the same tools the Pydoc uses (it’s just Python code, after all), but it’s a significant manual task.
A4 [中文] 可用 help('modules') 等文档工具（或 pkgutil 等；以教材所述为准）。

========================================================================
第 16 章  Function Basics
         函数基础
归属：Part IV 函数与生成器 Functions and Generators
级别：必读·核心    优先级：极高
========================================================================

【我的理解与总结】
函数目的、创建时机、默认返回 None、类型检查之弊。

【对你的帮助】
代码复用分水岭。

【建议掌握程度】
掌握约 95%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What is the point of coding functions?
Q1 [中文] 写函数的意义？
A1 [EN] Functions are the most basic way of avoiding code redundancy in Python—factoring code into functions means that we have only one copy of an operation’s code to update in the future. Functions are also the basic unit of code reuse in Python—wrapping code in functions makes it a reusable tool, callable in a variety of programs. Finally, functions allow us to divide a complex system into manageable parts, each of which may be developed individually. Functions are also used for object methods, but we’re postponing this role until we study classes.
A1 [中文] 减少重复、代码复用、把复杂系统拆成可管理部件。

── 第 2 题 ──
Q2 [EN] At what time does Python create a function?
Q2 [中文] Python 何时创建函数？
A2 [EN] A function is created when Python reaches and runs a def statement, which creates a function object and assigns it to the function’s name. This normally happens when the enclosing module file is imported by another module (recall that imports run the code in a file from top to bottom, including any def s), but it can also occur when a def is typed interactively or nested in other statements, such as if s. Functions are also created when a lambda is reached and run, though this doesn’t normally happen during an import operation.
A2 [中文] 运行到 def 时创建函数对象并绑定名字。

── 第 3 题 ──
Q3 [EN] What does a def function return if it has no return statement in it?
Q3 [中文] 无 return 时函数返回什么？
A3 [EN] A def function returns the None object by default if the control flow falls off the end of the function body without running into a return statement. Such functions are usually called with expression statements, as assigning their None results to variables is generally pointless. A return statement with no expression in it also returns None. A lambda implicitly returns its expression’s result, so it has no default.
A3 [中文] None。

── 第 4 题 ──
Q4 [EN] When does the code nested inside the function definition run?
Q4 [中文] 函数定义内部代码何时运行？
A4 [EN] The function body (the code nested inside the function def statement or following the colon in lambda) is run when the function is later invoked with a call expression. The body runs anew each time the function is called.
A4 [中文] 调用函数时，不是 def 时。

── 第 5 题 ──
Q5 [EN] What’s wrong with checking the types of objects passed into a function?
Q5 [中文] 检查传入对象类型有何不好？
A5 [EN] Checking the types of objects passed into a function effectively breaks the function’s flexibility, constraining the function to work on specific types only. Without such checks, the function would likely be able to process an entire array of object types—any objects that support the interface expected by the function will work. (The term interface means the set of methods and expression operators the function’s code runs.) 1 This code will always work if we intersect files’ contents obtained with file.readlines(). It may not work to intersect lines in open input files directly, though, depending on the file object’s implementation of the in operator or general iteration. Files must generally be rewound (e.g., with a file.seek(0) or another open) after they have been read to end-of-file once, and so are single-pass iterators. As you’ll see in Chapter 30 when we study operator overloading, objects implement the in operator either by providing the specific __contains__ method or by supporting the general iteration protocol with the __iter__ or older __getitem__ methods; classes can code these methods arbitrarily to define what iteration means for their data.
A5 [中文] 破坏多态，限制复用；应按接口/行为编程（除非有特殊需要）。

========================================================================
第 17 章  Scopes
         作用域
归属：Part IV 函数与生成器 Functions and Generators
级别：必读·核心    优先级：极高
========================================================================

【我的理解与总结】
LEGB、global/nonlocal、嵌套、状态保持。

【对你的帮助】
作用域是装饰器/闭包前置课。

【建议掌握程度】
掌握约 95%–100%（追踪输出题必会）。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What is the output of the following code, and why? >>> X = 'Hack' >>> def func(): print(X) >>> func()
Q1 [中文] 下列代码输出？为何？ X='Hack'; def func(): print(X); func()
A1 [EN] The output here is Hack because the function references a global variable in the enclosing module (because it is not assigned in the function, it is considered global).
A1 [中文] Hack。函数内未赋值，X 按 LEGB 找到模块全局。

── 第 2 题 ──
Q2 [EN] What is the output of this code, and why? >>> X = 'Hack' >>> def func(): X = 'Py!' >>> func() >>> print(X)
Q2 [中文] X='Hack'; def func(): X='Py!'; func(); print(X) 输出？
A2 [EN] The output here is Hack again because assigning the variable inside the function makes it a local and effectively hides the global of the same name. The print statement finds the variable unchanged in the global (module) scope.
A2 [中文] Hack。函数内赋值使 X 成局部，不改全局。

── 第 3 题 ──
Q3 [EN] What does this code print, and why? >>> X = 'Hack' >>> def func(): X = 'Py!' print(X) >>> func() >>> print(X)
Q3 [中文] 函数内赋值并 print(X)，函数外再 print ？
A3 [EN] It prints Py! on one line and Hack on another, because the reference to the variable within the function finds the assigned local and the reference in the print statement finds the global.
A3 [中文] 函数内打印 Py!，函数外仍 Hack。

── 第 4 题 ──
Q4 [EN] What output does this code produce? Why? >>> X = 'Hack' >>> def func(): global X X = 'Py!' >>> func() >>> print(X)
Q4 [中文] 使用 global X 后赋值再打印？
A4 [EN] This time it just prints Py! because the global declaration forces the variable assigned inside the function to refer to the variable in the enclosing global scope, even though the variable is assigned inside the function.
A4 [中文] Py!。global 使赋值改的是模块全局。

── 第 5 题 ──
Q5 [EN] What about this code—what’s the output, and why? >>> X = 'Hack' >>> def func(): X = 'Py!' def nested(): print(X) nested() >>> func() >>> print(X)
Q5 [中文] 嵌套函数打印外层函数局部 X？
A5 [EN] The output in this case is again Py! on one line and Hack on another, because the print statement in the nested function finds the name in the enclosing function’s local scope, and the display at the end finds the variable in the global scope.
A5 [中文] 内层 nested 打印 Py!（闭包/E 层）；全局仍 Hack。

── 第 6 题 ──
Q6 [EN] How about this example: what is its output, and why? >>> def func(): X = 'Py!' def nested(): nonlocal X X = 'Hack' nested() print(X) >>> func()
Q6 [中文] nonlocal X 在 nested 中赋值后外层 print？
A6 [EN] This example prints Hack because the nonlocal statement means that the assignment to X inside the nested function changes X in the enclosing function’s local scope. Without this statement, this assignment would classify X as local to the nested function, making it a different variable; the code would then print Py! instead.
A6 [中文] Hack。nonlocal 改的是外层函数的 X。

── 第 7 题 ──
Q7 [EN] Name three or more ways to retain state information across calls in a Python function.
Q7 [中文] 三种以上在多次调用间保留状态的方式？
A7 [EN] Although the values of local variables go away when a function returns, you can make a Python function retain state information by using shared global variables, nonlocal enclosing scope references within nested functions, or using default argument values. Function attributes also allow state to be attached to the function itself, instead of looked up in scopes. Another alternative, using classes and OOP, sometimes supports state retention better than any of the scope-based techniques because it makes it explicit with attribute assignments; we’ll explore this option in Part VI. Changing mutable objects in scopes and defaults works too, but may not be legal in some locales.
A7 [中文] 全局、函数属性、类实例、闭包 nonlocal、可变默认参数（慎用）等。

========================================================================
第 18 章  Arguments
         参数
归属：Part IV 函数与生成器 Functions and Generators
级别：必读·核心    优先级：极高
========================================================================

【我的理解与总结】
参数匹配追踪 + 可变对象传参副作用。

【对你的帮助】
API 与坑（默认可变参数）高发区。

【建议掌握程度】
掌握约 100%：每题输出都要能推。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What is the output of the following code, and why? >>> def func(a, b=4, c=5): print(a, b, c) >>> func(1, 2)
Q1 [中文] func(a,b=4,c=5); func(1,2) 输出？
A1 [EN] The output here is 1 2 5, because 1 and 2 are passed to a and b by position, and c is omitted in the call and hence defaults to 5.
A1 [中文] 1 2 5。位置传 a,b；c 用默认 5。

── 第 2 题 ──
Q2 [EN] What is the output of this code, and why? >>> def func(a, b, c=5): print(a, b, c) >>> func(1, c=3, b=2)
Q2 [中文] func(a,b,c=5); func(1,c=3,b=2) 输出？
A2 [EN] The output this time is 1 2 3 : 1 is passed to a by position, and b and c are passed 2 and 3 by name (the left-to-right order doesn’t matter when keyword arguments are used like this).
A2 [中文] 1 2 3。位置传 a，关键字传 b、c。

── 第 3 题 ──
Q3 [EN] How about this code: what is its output, and why? >>> def func(a, *pargs): print(a, pargs) >>> func(1, 2, 3)
Q3 [中文] func(a,*pargs); func(1,2,3) 输出？
A3 [EN] This code prints 1 (2, 3), because 1 is passed to a and the *pargs collects the remaining positional arguments into a new tuple object. We can step through the extra positional arguments tuple with any iteration tool (e.g., for arg in pargs: …).
A3 [中文] 1 (2, 3)。多余位置参数打进元组。

── 第 4 题 ──
Q4 [EN] What does this code print, and why? >>> def func(a, **kargs): print(a, kargs) >>> func(a=1, c=3, b=2)
Q4 [中文] func(a,**kargs); func(a=1,c=3,b=2) 输出？
A4 [EN] This time the code prints 1 {'c': 3, 'b': 2}, because 1 is passed to a by name and the **kargs collects the remaining keyword arguments into a dictionary. We could step through the extra keyword arguments dictionary by key with any iteration tool (e.g., for key in kargs: …). Note that the order of the dictionary’s keys reflects the order in which keyword arguments are passed, in recent Pythons.
A4 [中文] 1 {'c':3,'b':2}（键顺序依版本/插入）。多余关键字进字典。

── 第 5 题 ──
Q5 [EN] What gets printed by this, and why? >>> def func(a, b, c=3, d=4): print(a, b, c, d) >>> func(1, *(5, 6))
Q5 [中文] func(a,b,c=3,d=4); func(1,*(5,6)) 输出？
A5 [EN] The output here is 1 5 6 4 : the 1 matches a by position, 5 and 6 match b and c by * positional unpacking (6 overrides c ’s default), and d defaults to 4 because it was not passed a value.
A5 [中文] 1 5 6 4。1→a，解包 5,6→b,c；d 默认 4。

── 第 6 题 ──
Q6 [EN] One last time: what is the output of this code, and why? >>> def func(a, b, c): a = 2; b[0] = 'x'; c['a'] = 'y' >>> L=1; M=[1]; N={'a': 0} >>> func(L, M, N) >>> L, M, N
Q6 [中文] func 内改 a、b[0]、c['a'] 后 L,M,N？
A6 [EN] This displays (1, ['x'], {'a': 'y'}) —the first assignment in the function doesn’t impact the caller, but the second two do because they change passed-in mutable objects in place.
A6 [中文] L 仍为 1（不可变重绑局部）；M 为 ['x']；N 为 {'a':'y'}（可变对象共享被改）。

========================================================================
第 19 章  Function Odds and Ends
         函数杂项与进阶
归属：Part IV 函数与生成器 Functions and Generators
级别：必读·进阶    优先级：中高
========================================================================

【我的理解与总结】
lambda、map/filter/reduce、注解、递归、设计准则。

【对你的帮助】
函数工具箱。

【建议掌握程度】
掌握约 85%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] How are lambda expressions and def statements related?
Q1 [中文] lambda 与 def 关系？
A1 [EN] Both lambda and def create function objects to be called later. Because lambda is an expression, though, it returns a function object instead of assigning it to a name, and it can be used to nest a function definition in places where a def will not work syntactically. A lambda allows for only a single implicit return value expression, though; because it does not support a block of statements, it is not ideal for larger functions.
A1 [中文] 都创建函数对象；lambda 是表达式、匿名、只能单表达式，可出现在 def 语法不行处。

── 第 2 题 ──
Q2 [EN] What’s the point of using lambda ?
Q2 [中文] 使用 lambda 的意义？
A2 [EN] lambda allows us to “inline” small units of executable code, defer its execution, and provide it with state in the form of default arguments and enclosing scope variables. Using a lambda is never required; you can always code a def instead and reference the function by name. lambda comes in handy, though, to embed small pieces of deferred code that are unlikely to be used elsewhere in a program. It commonly appears in callback-based programs such as GUIs, and has a natural affinity with functional tools like map and filter that expect a processing function.
A2 [中文] 写短回调/内联函数，避免额外 def 名。

── 第 3 题 ──
Q3 [EN] Compare and contrast map, filter, and reduce.
Q3 [中文] 比较 map、filter、reduce。
A3 [EN] These three built-in functions all apply another function to items in a sequence (or other iterable) object and collect results. map passes each item to the function and collects call results, filter collects items for which the function returns a true value, and reduce computes a single value by applying the function to an accumulator and successive items. Unlike the other two, reduce is available in the functools module, not the built-in scope (in modern Python history, at least).
A3 [中文] map 映射；filter 过滤；reduce 归约累积（functools）。多可用推导式替代 map/filter。

── 第 4 题 ──
Q4 [EN] What are function annotations, and how are they used?
Q4 [中文] 函数注解是什么？如何用？
A4 [EN] Function annotations are syntactic embellishments of a function’s arguments and result, which are collected into a dictionary assigned to the function’s __annotations__ attribute. Python places no semantic meaning on these annotations, but simply packages them for potential use by other tools.
A4 [中文] 参数与返回值上的可选元数据，运行时不强制，供工具/文档。

── 第 5 题 ──
Q5 [EN] What are recursive functions, and how are they used?
Q5 [中文] 递归函数是什么？
A5 [EN] Recursive functions call themselves either directly or indirectly in order to loop (i.e., repeat). They may be used to traverse arbitrarily shaped structures, but they can also be used for iteration in general (though the latter role is often more simply and efficiently coded with looping statements). Recursion can often be simulated or replaced by code that uses explicit stacks or queues to have more control over traversals.
A5 [中文] 函数直接或间接调用自己；需基线条件。可表达分治，但 Python 有递归深度限制。

── 第 6 题 ──
Q6 [EN] What are some general design guidelines for coding functions?
Q6 [中文] 函数设计一般准则？
A6 [EN] Functions should generally be small and as self-contained as possible, have a single unified purpose, and communicate with other components through input arguments and return values. They may use mutable arguments to communicate results too if changes are expected, and some types of programs imply other communication mechanisms.
A6 [中文] 单一职责、明确输入输出、少副作用、少依赖全局、大小适中等。

── 第 7 题 ──
Q7 [EN] Name three or more ways that functions can communicate results to a caller.
Q7 [中文] 三种以上函数向调用方传达结果的方式？
A7 [EN] Functions can send back results with return statements, by changing passed-in mutable arguments, and by setting global variables. Globals are generally frowned upon (except for very special cases, like multithreaded programs) because they can make code more difficult to understand and use. return statements are usually best, but changing mutables is fine (and even useful), if expected. Functions may also communicate results with system devices such as files and sockets, but these are beyond our scope here.
A7 [中文] return、改可变参数、改全局、抛异常、写文件/输出等。

========================================================================
第 20 章  Comprehensions and Generations
         推导式与生成
归属：Part IV 函数与生成器 Functions and Generators
级别：必读·核心    优先级：极高
========================================================================

【我的理解与总结】
推导 vs 生成器表达式、yield、map 对比、async 初识。

【对你的帮助】
Pythonic 数据处理主力。

【建议掌握程度】
掌握约 95%（async 了解即可）。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What is the difference between enclosing a list comprehension in square brackets and parentheses?
Q1 [中文] 列表推导用[]与()的差别？
A1 [EN] List comprehensions in square brackets produce the result list all at once in memory. When they are enclosed in parentheses instead, they are actually generator expressions—they have the same internal syntax and a similar meaning but do not produce the result list all at once. Instead, generator expressions return a generator object, which yields one item in the result at a time when used in an iteration tool or iterated manually with next.
A1 [中文] [] 立即生成整表；() 是生成器表达式，惰性逐项产生。

── 第 2 题 ──
Q2 [EN] How are generators and iterators related?
Q2 [中文] 生成器与迭代器关系？
A2 [EN] Generators are iterable objects that support the iteration protocol automatically—they have an iterator with a __next__ method (run by next) that repeatedly advances to the next item in a series of results and raises an exception at the end of the series. In Python, we can code generator functions with def and yield, generator expressions with parenthesized comprehensions, and generator objects with classes that define a special method named __iter__ (discussed later in the book).
A2 [中文] 生成器是迭代器的一种：支持迭代协议，按需 yield 值。

── 第 3 题 ──
Q3 [EN] How can you tell if a function is a generator function?
Q3 [中文] 如何判断函数是生成器函数？
A3 [EN] A generator function has a yield statement (with or without its from extension) somewhere in its code. Generator functions are otherwise identical to normal functions syntactically, but they are compiled specially to return an iterable generator object when called. That object retains state and code location between values. (This means that deleting yield makes a function normal, but code deletions can cause all sorts of issues.)
A3 [中文] 函数体含 yield（调用返回生成器对象，不是一次跑完）。

── 第 4 题 ──
Q4 [EN] What does a yield statement do?
Q4 [中文] yield 做什么？
A4 [EN] When present, this statement makes Python compile the function specially as a generator; when called, the function returns a generator object that supports the iteration protocol. When the yield statement is run, it sends a result back to the caller and suspends the function’s state; the function can then be resumed after the last yield statement, in response to a next built-in or __next__ method call issued by the caller. In more advanced roles, the generator send method similarly resumes the generator, but can also pass a value that shows up as the yield expression’s value. Generator functions may also have a return statement, which terminates the generator (and attaches an optional value to the automatic StopIteration exception).
A4 [中文] 产出一个值并暂停函数状态，下次 next 从暂停处继续。

── 第 5 题 ──
Q5 [EN] How are map calls and list comprehensions related? Compare and contrast the two.
Q5 [中文] map 与列表推导比较？
A5 [EN] The map call is similar to a list comprehension—both produce a series of values, by collecting the results of applying an operation to each item in a sequence or other iterable, one item at a time. The primary difference is that map applies a function call to each item, and list comprehensions apply arbitrary expressions. Because of this, list comprehensions are more general; they can apply a function call expression like map, but map requires a function to apply other kinds of expressions. List comprehensions also support extended syntax—nested for loops, and if clauses that subsume the filter built-in. map also differs by producing a generator of values; the list comprehension materializes the result list in memory all at once, and this may matter for large lists.
A5 [中文] 都能映射；推导常更清晰灵活（含过滤）；对内置函数 map 在 CPython 有时更快。

── 第 6 题 ──
Q6 [EN] What do async def and await mean in a Python script?
Q6 [中文] async def 与 await 含义？
A6 [EN] The async def is used to define an asynchronous function (usually called a coroutine), and await waits for another asynchronous object to produce its value, possibly yielding control back to an event loop to allow other code to run during the wait. async def is required to use await, async for, and async with, and both an event loop and scheduling calls in module asyncio are generally required to achieve concurrency in this model. There’s more to the async story than told here; see Python’s manuals for next steps on this topic, as well as its multithreading and multiprocessing alternatives in Python’s standard library.
A6 [中文] 定义协程/异步函数，await 等待可等待对象；用于异步 I/O 并发（进阶）。

========================================================================
第 21 章  The Benchmarking Interlude
         性能测试插曲
归属：Part IV 函数与生成器 Functions and Generators
级别：选读·工具    优先级：中
========================================================================

【我的理解与总结】
各迭代工具与实现的相对速度结论。

【对你的帮助】
优化前先测量。

【建议掌握程度】
掌握约 70%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What conclusions can you draw from this chapter about the relative speed of Python iteration tools?
Q1 [中文] 关于 Python 迭代工具相对速度的结论？
A1 [EN] In CPython today, list comprehensions are often the quickest of the bunch; map beats list comprehensions in Python when all tools must call built-in functions; for loops tend to be slower than comprehensions; and generator functions and expressions are slower than all other options. Under PyPy, some of these findings differ; map often turns in a different relative performance, for example, and list comprehensions seem always quickest, perhaps due to function-level optimizations. At least that’s the case today on the Python versions tested, on the test machine used, and for the type of code timed—these results may vary if any of these three variables differ. Use the homegrown timer or standard library timeit to test your use cases for more relevant results. Also keep in mind that iteration is just one component of a program’s time: more code gives a more complete picture.
A1 [中文] CPython 上列表推导常很快；对内置函数 map 可能胜出；for 常更慢；生成器更省内存但常更慢。PyPy 下相对次序可能不同。

── 第 2 题 ──
Q2 [EN] What conclusions can you draw from this chapter about the relative speed of the Pythons timed?
Q2 [中文] 关于所测各 Python 实现速度的结论？
A2 [EN] In general, PyPy 7.3 (implementing Python 3.10) is substantially faster than CPython 3.12. In some cases timed, PyPy was 5X–20X faster than CPython, though in isolated cases (e.g., some string operations), PyPy was slower than CPython, and PyPy’s use of a JIT can impact benchmark results. At least that’s the case today on the Python versions tested, on the test machine used, and for the type of code timed—these results may vary if any of these three variables differ. Use the homegrown timer or standard library timeit to test your use cases for more relevant results. This is especially true when timing Python implementations, which may be arbitrarily optimized in each new release—in fact, CPython may soon adopt a JIT like PyPy, which could invalidate results here. We also didn’t test any of the many other Python implementations; see Chapter 2 for other options to time on your own.
A2 [中文] 实现不同性能画像不同（如 PyPy 对长期运行的纯 Python 循环常更优）。以本章实测为准，优化靠测量。

========================================================================
第 22 章  Modules: The Big Picture
         模块：全局图景
归属：Part V  模块与包 Modules and Packages
级别：必读·核心    优先级：高
========================================================================

【我的理解与总结】
模块如何变成对象、PYTHONPATH、搜索路径、命名空间。

【对你的帮助】
多文件项目理论。

【建议掌握程度】
掌握约 90%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] How does a module source code file become a module object?
Q1 [中文] 模块源文件如何变成模块对象？
A1 [EN] A module’s source code file automatically becomes a module object when that module is imported. Technically, the module’s source code is run during the import, one statement at a time, and all the names assigned in the process become attributes of the generated module object.
A1 [中文] import 时运行源码，顶层赋值的名字成为模块对象的属性。

── 第 2 题 ──
Q2 [EN] Why might you have to set your PYTHONPATH environment variable?
Q2 [中文] 为何可能要设 PYTHONPATH？
A2 [EN] You need to set PYTHONPATH only to import from directories other than Python’s standard library and the “home” directory—the current directory when working interactively, or the directory containing your program’s top-level file. In practice, this might be required for nontrivial programs that use libraries of tools.
A2 [中文] 以便从标准库与“主目录”之外的目录导入。

── 第 3 题 ──
Q3 [EN] Name the five major components of the module search path.
Q3 [中文] 模块搜索路径五大组成部分？
A3 [EN] The five major components of the module search path are the top-level script’s home directory (the directory containing it), all directories listed in the PYTHONPATH environment variable, the standard-library directories, the site-packages root directory for third-party extension installs, and all directories listed in.pth path files located in standard places. Of these, programmers can customize PYTHONPATH and.pth files.
A3 [中文] 通常包括：程序主目录、PYTHONPATH 目录、标准库目录、.pth 文件内容、站点包等（以教材列表为准）。

── 第 4 题 ──
Q4 [EN] Name four file types that Python might load in response to an import operation.
Q4 [中文] import 可能加载的四种文件类型？
A4 [EN] Python might load a source code (.py) file, a bytecode (.pyc) file, a C extension module, or a directory of the same name for package imports. Imports may also load more exotic things such as ZIP file components, Java classes under the Jython version of Python,.NET components under IronPython, and statically linked C modules that have no files present at all. In fact, with import extensions, imports can load nearly arbitrary items.
A4 [中文] 源码 .py、字节码 .pyc、扩展模块（如 .pyd/.so）、包目录等。

── 第 5 题 ──
Q5 [EN] What is a module namespace, and what does a module’s namespace contain?
Q5 [中文] 什么是模块命名空间？含什么？
A5 [EN] A module namespace is a self-contained package of variables, which are known as the attributes of the module object. A module’s namespace contains all the names assigned by code at the top level of the module file (i.e., not nested in def or class statements). A module’s global scope morphs into the module object’s attributes namespace. A module’s namespace may also be altered by assignments from other files that import it, though this is generally frowned upon (see Chapter 17 for more on the downsides of cross-file changes).
A5 [中文] 模块的属性字典空间，含导入时执行顶层代码所赋值的名字。

========================================================================
第 23 章  Module Coding Basics
         模块编码基础
归属：Part V  模块与包 Modules and Packages
级别：必读·核心    优先级：极高
========================================================================

【我的理解与总结】
创建模块、from/import/reload、from 陷阱。

【对你的帮助】
会拆模块写库。

【建议掌握程度】
掌握约 95%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] How do you make a module?
Q1 [中文] 如何制作模块？
A1 [EN] To create a module, you simply write a text file containing Python statements; every source code file is automatically a module, and there is no syntax for declaring one. Import operations load module files into module objects in memory. You can also make a module by writing code in an external language like C or Java, but such extension modules are beyond the scope of this book (and most of its readers).
A1 [中文] 写包含 Python 语句的 .py 文件即可，无需特殊声明。

── 第 2 题 ──
Q2 [EN] How is the from statement related to the import statement?
Q2 [中文] from 与 import 关系？
A2 [EN] The from statement imports an entire module, like the import statement, but as an extra step, it also copies one or more variables from the imported module into the scope where the from appears. This enables you to use the imported names directly (name) instead of having to go through the module (module.name).
A2 [中文] from 先 import 模块，再把名字复制到本地作用域；对象仍共享。

── 第 3 题 ──
Q3 [EN] How is the reload function related to imports?
Q3 [中文] reload 与 import 关系？
A3 [EN] By default, a module is imported only once per process. The reload function forces a module to be imported again. It is mostly used to pick up new versions of a module’s source code during development, and in dynamic customization scenarios where users change part of a system without restarting it.
A3 [中文] import 默认一次；reload 强制重新执行模块源码更新已有模块对象。

── 第 4 题 ──
Q4 [EN] When must you use import instead of from ?
Q4 [中文] 何时必须用 import 而不是 from？
A4 [EN] You must use import instead of from only when you need to access the same name in two different modules. To make the two names unique, qualify with the names of their enclosing modules obtained with import. The as extension can render from usable in this context as well, by renaming imports uniquely.
A4 [中文] 需要模块对象本身、或同名冲突需用模块名限定、或要 reload 等场景。

── 第 5 题 ──
Q5 [EN] Name three potential pitfalls of the from statement.
Q5 [中文] from 的三个潜在陷阱？
A5 [EN] The from statement can obscure the meaning of a variable (which module it is defined in), can have problems with the reload call (names may reference prior versions of objects), and can corrupt namespaces (it might silently overwrite names you are using in your scope). The from * form is worse in most regards—it can corrupt namespaces arbitrarily and obscure the meaning of variables, so it is probably best used sparingly.
A5 [中文] 名字含义变模糊、可能覆盖已有名、from * 污染命名空间、对 reload 不友好等。

========================================================================
第 24 章  Module Packages
         模块包
归属：Part V  模块与包 Modules and Packages
级别：必读·核心    优先级：高
========================================================================

【我的理解与总结】
__init__.py、包路径、相对导入、命名空间包。

【对你的帮助】
中大型目录结构。

【建议掌握程度】
掌握约 85%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What is the purpose of an __init__.py file in a module package directory?
Q1 [中文] __init__.py 在包目录中的目的？
A1 [EN] The __init__.py file serves to declare and initialize a regular module package; Python automatically runs its code the first time you import through a directory in a process. Its assigned variables become the attributes of the module object created in memory to correspond to that directory. It’s optional for package folders but gives a folder search precedence over other folders of the same name that don’t have this file.
A1 [中文] 声明/初始化常规包；首次导入时执行；其赋值成包属性；也影响搜索优先级。

── 第 2 题 ──
Q2 [EN] How can you avoid repeating the full package path every time you reference a package’s content?
Q2 [中文] 如何避免每次写完整包路径？
A2 [EN] Use the from statement with a package to copy names out of the package directly, or use the as extension with the import statement to rename the path to a shorter synonym. In both cases, the path is listed in only one place, in the from or import statement.
A2 [中文] from 导入到本地、或 import as 短名、在包 __init__ 中再导出等。

── 第 3 题 ──
Q3 [EN] Which directories require __init__.py files?
Q3 [中文] 哪些目录需要 __init__.py？
A3 [EN] Trick question! These files used to be required for packages in earlier Pythons but became optional with accommodations for namespace packages in the module search algorithm. As noted in answer 1, though, these folders still have valid, if optional, roles, including boosting a folder’s import-search precedence.
A3 [中文] 传统常规包目录需要；命名空间包等场景可例外（教材所述）。

── 第 4 题 ──
Q4 [EN] When must you use import instead of from with packages?
Q4 [中文] 包场景何时必须 import 而非 from？
A4 [EN] You must use import instead of from with packages only if you need to access the same name defined in more than one path. With import, the path makes the references unique, but from allows only one version of any given name (unless you also use the as extension to rename uniquely).
A4 [中文] 需要完整路径限定避免歧义、或要包对象本身等（同模块原则）。

── 第 5 题 ──
Q5 [EN] What is the difference between from pkg import name and from. import name ?
Q5 [中文] from pkg import name 与 from . import name 差别？
A5 [EN] The from pkg import name is an absolute import—the search for pkg skips an enclosing package and then tries the “absolute” directories in sys.path. A statement from. import name, on the other hand, is a relative import— name is looked up relative to the package in which this statement is contained, only.
A5 [中文] 前者绝对（从 sys.path 找 pkg）；后者相对当前包。

── 第 6 题 ──
Q6 [EN] What is a namespace package?
Q6 [中文] 什么是命名空间包？
A6 [EN] A namespace package is an extension to the import model that corresponds to one or more directories that do not have __init__.py files. When Python finds these during an import search and does not find a simple module or regular package first, it creates a namespace package that is the virtual concatenation of all found directories having the requested module name. Further nested components are looked up in all the namespace package’s directories. The effect is similar to a regular package, but content may be split across multiple directories.
A6 [中文] 可跨多目录的包机制，不一定要 __init__.py（高级）。

========================================================================
第 25 章  Module Odds and Ends
         模块杂项与进阶
归属：Part V  模块与包 Modules and Packages
级别：选读·进阶    优先级：中
========================================================================

【我的理解与总结】
_ 前缀、__main__、属性遍历、动态导入、__future__ 玩笑题。

【对你的帮助】
模块实践细节。

【建议掌握程度】
掌握约 80%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What is significant about variables at the top level of a module whose names begin with a single underscore?
Q1 [中文] 模块顶层单下划线开头变量有何意义？
A1 [EN] Variables at the top level of a module whose names begin with a single underscore are not copied out to the importing scope when the from * statement form is used. They can still be accessed by an import or the normal from statement form, though. The __all__ list is similar, but the logical converse; its contents are the only names that are copied out for a from *.
A1 [中文] from * 默认不导出它们；仍可用 import 或普通 from 访问。__all__ 则正向列出可导出名。

── 第 2 题 ──
Q2 [EN] What does it mean when a module’s __name__ variable is the string '__main__' ?
Q2 [中文] 模块 __name__ 为 '__main__' 意味着什么？
A2 [EN] If a module’s __name__ variable is the string '__main__', it means that the file is being executed as a top-level script instead of being imported from another file in the program. That is, the file is being used as a program, not a library. This usage mode variable supports dual-mode code and tests.
A2 [中文] 该文件被直接运行而非导入；常用于脚本入口测试代码。

── 第 3 题 ──
Q3 [EN] How might you step through all the attributes in a module with a loop?
Q3 [中文] 如何用循环遍历模块所有属性？
A3 [EN] By using the module’s built-in __dict__ attribute. This is a normal dictionary that holds all of the module’s attributes, so code can iterate over its keys, values, or key/value pairs. When needed, attribute values can also be fetched for string names by indexing __dict__, or calling the getattr built-in function.
A3 [中文] for name in dir(mod): ... 或遍历 mod.__dict__。

── 第 4 题 ──
Q4 [EN] If the user interactively types the name of a module to test, how can your code import it?
Q4 [中文] 用户交互输入模块名时如何导入？
A4 [EN] User input usually comes into a script as a string; to import the referenced module given its string name, you can build and run an import statement with exec, or pass the string name in a call to the __import__ or importlib.import_module functions.
A4 [中文] 用 importlib.import_module(name) 或 __import__ 等动态导入。

── 第 5 题 ──
Q5 [EN] If the module __future__ allows us to import from the future, can we also import from the past?
Q5 [中文] 能从“过去” import 吗？
A5 [EN] No, we can’t import from the past in Python. We can install (or stubbornly use) an older version of the language, but the latest Python is generally the best Python (with apologies to 2.X fans in the audience).
A5 [中文] 幽默题：__future__ 可启用未来特性；不能真的从过去导入。历史兼容靠旧版本/工具。

========================================================================
第 26 章  OOP: The Big Picture
         面向对象：全局图景
归属：Part VI 类与 OOP Classes and OOP
级别：必读·核心    优先级：极高
========================================================================

【我的理解与总结】
OOP 目的、继承搜索、类vs实例、self、__init__。

【对你的帮助】
OOP 世界观。

【建议掌握程度】
掌握约 90%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What is the main point of OOP in Python?
Q1 [中文] Python 中 OOP 的要点？
A1 [EN] OOP is about code reuse —you factor code to minimize redundancy, and program by customizing what already exists instead of changing code in place or starting from scratch.
A1 [中文] 代码复用：通过定制已有代码减少冗余，而非处处从零修改。

── 第 2 题 ──
Q2 [EN] Where does an inheritance search look for an attribute?
Q2 [中文] 继承搜索在哪找属性？
A2 [EN] An inheritance search looks for an attribute first in the instance object, then in the class the instance was created from, then in all higher superclasses, progressing from the bottom to the top of the object tree, and from left to right (normally). The search stops at the first place the attribute is found. Because the lowest version of a name found along the way wins, class hierarchies naturally support customization by extension in new subclasses.
A2 [中文] 先实例，再其类，再超类树（由下而上；新式类按 MRO）。

── 第 3 题 ──
Q3 [EN] What is the difference between a class object and an instance object?
Q3 [中文] 类对象与实例对象差别？
A3 [EN] Both class and instance objects are namespaces—packages of variables that appear as attributes. The main difference between them is that classes are a kind of factory for creating multiple instances. Classes also support operator-overloading methods, which instances inherit, and treat any functions nested in the class as methods for processing instances.
A3 [中文] 类是工厂/类型；实例是具体对象，各有自己的状态。

── 第 4 题 ──
Q4 [EN] Why is the first argument in a class’s method function special?
Q4 [中文] 方法第一个参数为何特殊？
A4 [EN] The first argument in a class’s method function is special because it always receives the instance object that is the implied subject of the method call. It’s usually called self by convention. Because method functions always have this implied subject—and object context—by default, we say they are “object-oriented” (i.e., designed to process or change objects).
A4 [中文] self（惯例名）接收调用方法的实例。

── 第 5 题 ──
Q5 [EN] What is the __init__ method used for?
Q5 [中文] __init__ 用途？
A5 [EN] If the __init__ method is coded or inherited in a class, Python calls it automatically each time an instance of that class is created. It’s known as the constructor method; it is passed the new instance implicitly, as well as any arguments passed explicitly to the class name. It’s also the most commonly used operator-overloading method. If no __init__ method is present, instances simply begin life as empty namespaces.
A5 [中文] 构造实例时初始化属性。

── 第 6 题 ──
Q6 [EN] How do you create a class instance?
Q6 [中文] 如何创建实例？
A6 [EN] You create a class instance by calling the class name as though it were a function; any arguments passed into the class name show up as arguments two and beyond in the __init__ constructor method (if there is one). The new instance remembers the class it was created from for inheritance purposes.
A6 [中文] 调用类：ClassName(...)。

── 第 7 题 ──
Q7 [EN] How do you create a class?
Q7 [中文] 如何创建类？
A7 [EN] You create a class by running a class statement; like function definitions, these statements normally run when the enclosing module file is imported (more on this in the next chapter).
A7 [中文] 执行 class 语句。

── 第 8 题 ──
Q8 [EN] How do you specify a class’s superclasses?
Q8 [中文] 如何指定超类？
A8 [EN] You specify a class’s superclasses by listing them in parentheses in the class statement, after the new class’s name. The left-to-right order in which the classes are listed in the parentheses gives the left-to-right inheritance search order in the class tree. 1 In other literature and circles, you may also occasionally see the terms base classes and derived classes used to describe superclasses and subclasses, respectively. Most Python people and this book tend to use the latter terms. 2 If you’ve ever used C++ or Java, you’ll recognize that Python’s self is much like these languages’ this pointer/reference, but self is always explicit in both headers and bodies of Python methods to make attribute accesses more obvious: a name has fewer possible meanings to consider, if it cannot be magically associated with a hidden object. Explicit is generally better.
A8 [中文] class 头：class Sub(Super1, Super2):。

========================================================================
第 27 章  Class Coding Basics
         类编码基础
归属：Part VI 类与 OOP Classes and OOP
级别：必读·核心    优先级：极高
========================================================================

【我的理解与总结】
类与模块、属性创建位置、重载、OOP 三概念。

【对你的帮助】
能手写简单类。

【建议掌握程度】
掌握约 95%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] How are classes related to modules?
Q1 [中文] 类与模块关系？
A1 [EN] Classes are always nested inside a module; they are attributes of a module object. Classes and modules are both namespaces, but classes correspond to statements (not entire files) and support the OOP notions of multiple instances, inheritance, and operator overloading (modules mostly do not). In a sense, a module is like a single-instance class, without inheritance, which corresponds to an entire file of code.
A1 [中文] 类嵌在模块中，是模块属性；二者都是命名空间，但类支持多实例、继承、重载。

── 第 2 题 ──
Q2 [EN] How are instances and classes created?
Q2 [中文] 实例与类如何创建？
A2 [EN] Classes are made by running class statements; instances are created by calling a class as though it were a function.
A2 [中文] class 语句创建类；调用类创建实例。

── 第 3 题 ──
Q3 [EN] Where and how are class attributes created?
Q3 [中文] 类属性在哪如何创建？
A3 [EN] Class attributes are created by assigning attributes to a class object. They are normally generated by top-level assignments nested in a class statement—each name assigned in the class statement block becomes an attribute of the class object (technically, the class statement’s local scope morphs into the class object’s attribute namespace, much like a module). Class attributes can also be created, though, by assigning attributes to the class anywhere a reference to the class object exists— even outside the class statement.
A3 [中文] class 语句顶层赋值；或之后 Class.attr=...。

── 第 4 题 ──
Q4 [EN] Where and how are instance attributes created?
Q4 [中文] 实例属性在哪如何创建？
A4 [EN] Instance attributes are created by assigning attributes to an instance object. They are normally created within a class’s method functions coded inside the class statement, by assigning attributes to the self argument (which is always the implied instance). Again, though, they may be created by assignment anywhere a reference to the instance appears, even outside the class statement. Usually, all instance attributes are initialized in the __init__ constructor method; that way, later method calls can assume the attributes already exist.
A4 [中文] 通常在 __init__ 或方法里 self.attr=...；也可实例上直接赋。

── 第 5 题 ──
Q5 [EN] What does self mean in a Python class?
Q5 [中文] self 是什么意思？
A5 [EN] self is the name commonly given to the first (leftmost) argument in a class’s method function; Python automatically fills it in with the instance object that is the implied subject of the method call. This argument need not be called self (though this is a very strong convention); its position is what is significant. (Ex-C++ or Java programmers might prefer to call it this because in those languages that name reflects the same idea; in Python, though, this argument must always be explicit in method headers and code.)
A5 [中文] 方法中指向当前实例的参数。

── 第 6 题 ──
Q6 [EN] How is operator overloading coded in a Python class?
Q6 [中文] 如何编码运算符重载？
A6 [EN] Operator overloading is coded in a Python class with specially named methods; they all begin and end with double underscores to make them unique. These are not built-in or reserved names; Python just runs them automatically when an instance appears in the corresponding operation. Python itself defines the mappings from operations to special method names.
A6 [中文] 定义双下划线特殊方法，如 __add__、__str__。

── 第 7 题 ──
Q7 [EN] When might you want to support operator overloading in your classes?
Q7 [中文] 何时支持运算符重载？
A7 [EN] Operator overloading is useful to implement objects that resemble built- in types (e.g., sequences or numeric objects such as matrixes), and to mimic the built-in type interface expected by a piece of code. Mimicking built-in type interfaces enables you to pass in class instances that also have state information (i.e., attributes that remember data between operation calls). You generally shouldn’t use operator overloading when a simple named method will suffice, though.
A7 [中文] 希望对象像内置类型一样自然使用时。

── 第 8 题 ──
Q8 [EN] Which operator-overloading method is most commonly used?
Q8 [中文] 最常用的重载方法？
A8 [EN] The __init__ constructor method is the most commonly used; almost every class uses this method to set initial values for instance attributes and perform other startup tasks. The __str__ method (and its __repr__ sibling) is not uncommon, but not as much of a fixture as __init__.
A8 [中文] __init__（以及常用 __repr__/__str__）。

── 第 9 题 ──
Q9 [EN] What are the three key concepts required to understand Python OOP code?
Q9 [中文] 理解 Python OOP 的三个关键概念？
A9 [EN] The special self argument in method functions, the inheritance search for attributes, and the __init__ constructor method are the cornerstones of OOP code in Python. If you get these, you should be able to read the text of most OOP Python code—apart from these, it’s largely just packages of functions. In classes, self represents the automatic object argument, and __init__ is commonly used to initialize instances. 1 Not to be confused with the __init__.py files in module packages! The method here is a class constructor function used to initialize the newly created instance, not a namespace for a module- package folder. See Chapter 24 for more details. 2 In fact, this is one of the reasons the self argument must always be explicit in Python methods— because methods can be created as simple functions independent of a class, they need to include the implied instance argument explicitly. They can be called as either functions or methods, and Python can neither guess nor assume that a simple function might eventually become a class’s method. The main reason for the explicit self argument, though, is to make the meanings of names more apparent: names not referenced through self are simple variables mapped to scopes, while names referenced through self with attribute notation are obviously instance attributes.
A9 [中文] 继承、组合/委托、运算符重载（及实例与类命名空间）——以教材三要点为准：继承、自适应/多态、运算符重载等表述。

========================================================================
第 28 章  A More Realistic Example
         更贴近实际的例子
归属：Part VI 类与 OOP Classes and OOP
级别：必读·应用    优先级：高
========================================================================

【我的理解与总结】
用持久化人员例子串起继承、方法、组合、设计原则。

【对你的帮助】
从语法到小系统设计。

【建议掌握程度】
掌握约 85%：能讲清每题设计理由。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] When we fetch a Manager object from the shelf and print it, where does the display format logic come from?
Q1 [中文] 从 shelf 取 Manager 打印时，显示格式逻辑从哪来？
A1 [EN] In the final version of our classes, Manager ultimately inherits its __repr__ printing method from AttrDisplay in the separate classtools module, and two levels up in the class tree. Manager doesn’t have one itself, so the inheritance search climbs to its Person superclass; because there is no __repr__ there either, the search climbs higher and finds it in AttrDisplay. The class names listed in parentheses in a class statement’s header line provide the links to higher superclasses.
A1 [中文] 继承链上的 __repr__（如 AttrDisplay），Manager→Person→工具类。

── 第 2 题 ──
Q2 [EN] When we fetch a Person object from a shelf without importing its module, how does the object know that it has a giveRaise method that we can call?
Q2 [中文] 未 import 其模块却从 shelf 取出 Person，怎知有 giveRaise？
A2 [EN] Shelves (really, the pickle module they use) automatically relink an instance to the class it was created from when that instance is later loaded back into memory. Python reimports the class from its module internally and creates a new instance with the previously stored attributes. This way, loaded instances automatically obtain all their original methods (like lastName, giveRaise, and __repr__), even if we have not imported the instance’s class into the loading scope.
A2 [中文] pickle 等重建实例时会带上类信息并自动导入类所在模块，方法在类上。

── 第 3 题 ──
Q3 [EN] Why is it so important to move processing into methods, instead of hardcoding it outside the class?
Q3 [中文] 为何把处理放进方法而不是类外写死？
A3 [EN] It’s important to move processing into methods so that there is only one copy to change in the future, and so that the methods can be run on any instance. This is Python’s notion of encapsulation —wrapping up logic behind interfaces, to better support future code maintenance. If you don’t do so, you create code redundancy that can multiply your work effort as the code evolves in the future.
A3 [中文] 封装变化、利复用与定制，改一处胜过改多处脚本。

── 第 4 题 ──
Q4 [EN] Why is it better to customize by subclassing rather than copying the original and modifying?
Q4 [中文] 为何用子类定制优于复制修改？
A4 [EN] Customizing with subclasses reduces development effort. In OOP, we code by customizing what has already been done, rather than copying or changing existing code. This is the real “big idea” in OOP—because we can easily extend our prior work by coding new subclasses, we can leverage what we’ve already done. This is much better than either starting from scratch each time, or introducing multiple redundant copies of code that may all have to be updated in the future.
A4 [中文] 减少重复，原类改进自动惠及子类，结构更清晰。

── 第 5 题 ──
Q5 [EN] Why is it better to call back to a superclass method to run default actions, instead of copying and modifying its code in a subclass?
Q5 [中文] 为何回调超类方法跑默认动作优于复制代码？
A5 [EN] Copying and modifying code doubles your potential work effort in the future, regardless of the context. If a subclass method needs to perform default actions coded in a superclass method, it’s much better to call back to the original through the superclass’s name (or the super built- in) than to copy its code. This also holds true for superclass constructors. Again, copying code creates redundancy, which is a major issue as code evolves.
A5 [中文] 共享维护；超类修复/增强自动生效。

── 第 6 题 ──
Q6 [EN] Why is it better to use tools like __dict__ that allow objects to be processed generically than to write more custom code for each type of class?
Q6 [中文] 为何用 __dict__ 等通用工具优于每类手写定制？
A6 [EN] Generic tools can avoid hardcoded solutions that must be kept in sync with the rest of the class as it evolves over time. A generic __repr__ print method, for example, need not be updated each time a new attribute is added to instances in an __init__ constructor. In addition, a generic print method inherited by all classes appears and need be modified in only one place—changes in the generic version are picked up by all classes that inherit from the generic class. Again, eliminating code redundancy cuts future development effort; that’s one of the primary assets classes bring to the table.
A6 [中文] 减少样板代码，一套逻辑处理多种对象。

── 第 7 题 ──
Q7 [EN] In general terms, when might you choose to use object embedding and composition instead of inheritance?
Q7 [中文] 何时选嵌入组合而非继承？
A7 [EN] Inheritance is best at coding extensions based on direct customization (like our Manager specialization of Person). Composition is well suited to scenarios where multiple objects are aggregated into a whole and directed by a controller-layer class. Inheritance passes calls up to reuse, and composition passes down to delegate. Inheritance and composition are not mutually exclusive; often, the objects embedded in a controller are themselves customizations based upon inheritance.
A7 [中文] 是 has-a 关系、需运行时装配、或继承树会别扭时。

── 第 8 题 ──
Q8 [EN] What would you have to change if the objects coded in this chapter used a dictionary for names and a list for jobs, as in similar examples earlier in this book?
Q8 [中文] 若名字用字典、工作用列表要改什么？
A8 [EN] Not much since this was really a first-cut prototype, but the lastName method would need to be updated for the new name format; the Person constructor would have to change the job default to an empty list; and the Manager class would probably need to pass along a job list in its constructor instead of a single string (self-test code would change as well, of course). The good news is that these changes would need to be made in just one place—in our classes, where such details are encapsulated. Apart from their object-construction calls, the database scripts should work as is, as shelves support arbitrarily nested data.
A8 [中文] 属性存储与访问方式、打印/序列化相关代码等（按数据结构改接口）。

── 第 9 题 ──
Q9 [EN] How might you modify the classes in this chapter to implement a personal contacts database in Python?
Q9 [中文] 如何改成个人联系人数据库？
A9 [EN] The classes in this chapter could be used as boilerplate “template” code to implement a variety of types of databases. Essentially, you can repurpose them by modifying the constructors to record different attributes and providing whatever methods are appropriate for the target application. For instance, you might use attributes such as name, address, birthday, phone, email, and so on for a contacts database, and methods appropriate for this purpose. A method named sendmail, for example, might use Python’s standard-library smtplib module to send an email to one of the contacts automatically when run (see Python’s manuals or application-level books for more details on such tools). The AttrDisplay tool we wrote here could be used verbatim to print your objects because it is intentionally generic. Most of the shelve database code here can be used to store your objects, too, with minor changes. 1 And no offense to any managers in the audience, of course. This joke was once delivered at a Python class in New Jersey, and nobody laughed. The organizers later revealed that the attendees were all managers evaluating Python. Hence the silence. 2 Terminology note: this book now uses the noun “shelf” for the object storage managed by module shelve, and its plural “shelves” for more than one “shelf” managed by shelve. In the past, the module’s verb name “shelve” was also confusingly used as the noun—much to the chagrin of this book’s editors over the years, both electronic and human.
A9 [中文] 调整字段与方法（电话/邮箱等），持久化 shelf/数据库，提供查询 UI 或脚本。

========================================================================
第 29 章  Class Coding Details
         类编码细节
归属：Part VI 类与 OOP Classes and OOP
级别：必读·核心    优先级：高
========================================================================

【我的理解与总结】
抽象超类、类级赋值、super/__init__ 链、增强方法、类作用域。

【对你的帮助】
类细节与框架思维。

【建议掌握程度】
掌握约 85%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What is an abstract superclass?
Q1 [中文] 什么是抽象超类？
A1 [EN] An abstract superclass is a class that calls a method, but does not inherit or define it—it expects the method to be filled in by a subclass. This is often used as a way to generalize classes when behavior cannot be predicted until a more specific subclass is coded. OOP frameworks also use this as a way to dispatch to client-defined, customizable operations.
A1 [中文] 调用某方法但不定义/实现它，期望子类提供；用于框架回调与泛化。

── 第 2 题 ──
Q2 [EN] What happens when a simple assignment statement appears at the top level of a class statement?
Q2 [中文] class 顶层简单赋值会发生什么？
A2 [EN] When a simple assignment statement (X = Y) appears at the top level of a class statement, it attaches a data attribute to the class (Class.X). Like all class attributes, this will be shared by all instances that do not have the same attribute. Methods are generally created instead by def statements nested in a class.
A2 [中文] 创建类属性（由所有实例共享，除非实例遮盖）。

── 第 3 题 ──
Q3 [EN] Why might a class need to manually call the __init__ method in a superclass?
Q3 [中文] 为何类可能需手动调用超类 __init__？
A3 [EN] A class must manually call the __init__ method in a superclass if it defines an __init__ constructor of its own and still wants the superclass’s construction code to run (and it often will). Python itself automatically runs just one constructor—the lowest one in the inheritance tree. Superclass constructors are often called through the class name, passing in the self instance manually: Superclass.__init__(self, …); they may also be called by super().__init__(…), though we haven’t yet studied this form in full.
A3 [中文] Python 不会自动链式调所有超类构造；子类要显式 Super.__init__(self,...) 或 super()。

── 第 4 题 ──
Q4 [EN] How can you augment, instead of completely replacing, an inherited method?
Q4 [中文] 如何增强而非完全替换继承方法？
A4 [EN] To augment instead of completely replacing an inherited method, redefine it in a subclass, but call back to the superclass’s version of the method manually from the new version of the method in the subclass. That is, pass the self instance to the superclass’s version of the method manually: Superclass. method (self, …); or do so implicitly with super(). method (…). The prior answer is really just a special case of this one.
A4 [中文] 在子类方法中调用超类同名方法，再追加行为。

── 第 5 题 ──
Q5 [EN] How does a class’s local scope differ from that of a function?
Q5 [中文] 类的局部作用域与函数有何不同？
A5 [EN] A class is a local scope and has access to enclosing local scopes, but it does not serve as an enclosing local scope to further nested code. Like modules, the class local scope morphs into an attribute namespace after the class statement is run.
A5 [中文] class 顶层作用域在 class 语句期间存在；方法是函数，遵循 LEGB，不会把类块当普通局部嵌套作用域用（需 Class.name 或 self）。

========================================================================
第 30 章  Operator Overloading
         运算符重载
归属：Part VI 类与 OOP Classes and OOP
级别：必读·进阶    优先级：高
========================================================================

【我的理解与总结】
迭代重载、打印重载、切片、+=、何时重载。

【对你的帮助】
让对象用起来像内置类型。

【建议掌握程度】
掌握约 85%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What two operator-overloading methods can you use to support iteration in your classes?
Q1 [中文] 支持迭代的两个重载方法？
A1 [EN] Classes can support iteration by defining (or inheriting) __getitem__ or __iter__. In all iteration tools, Python tries to use __iter__ first, which returns an object that supports the iteration protocol with a __next__ method: if no __iter__ is found by inheritance search, Python falls back on the __getitem__ indexing method, which is called repeatedly, with successively higher indexes. If used, the yield statement can create the __next__ method automatically.
A1 [中文] __iter__（优先，返回带 __next__ 的迭代器）与 __getitem__（按索引回退）。

── 第 2 题 ──
Q2 [EN] What two operator-overloading methods handle printing, and in what contexts?
Q2 [中文] 处理打印的两个方法及场景？
A2 [EN] The __str__ and __repr__ methods implement object print displays. The former is called by the print and str built-in functions; the latter is called by print and str if there is no __str__, and always by the repr built-in, interactive echoes, and nested appearances. That is, __repr__ is used everywhere, except by print and str when a __str__ is defined. A __str__ is usually used for user-friendly displays; __repr__ gives extra details or the object’s as-code form. String formatting runs these methods too.
A2 [中文] __str__（str/print 用户友好）、__repr__（repr/调试/交互显示）。

── 第 3 题 ──
Q3 [EN] How can you intercept slice operations in a class?
Q3 [中文] 如何拦截切片？
A3 [EN] Slicing is caught by the __getitem__ indexing method: it is called with a slices object instead of a simple integer index, and slice objects may be passed on or inspected as needed.
A3 [中文] __getitem__（传入 slice 对象）；赋值还有 __setitem__ 等。

── 第 4 题 ──
Q4 [EN] How can you catch in-place addition in a class?
Q4 [中文] 如何捕获原地加法？
A4 [EN] In-place addition tries __iadd__ first, and __add__ with an assignment second. The same policy holds true for all binary operators. The __radd__ method is also available for right-side addition.
A4 [中文] __iadd__；若无则可能退回 __add__ 再重绑。

── 第 5 题 ──
Q5 [EN] When should you provide operator overloading?
Q5 [中文] 何时提供运算符重载？
A5 [EN] When a class naturally matches, or needs to emulate, a built-in type’s interfaces. For example, collections might imitate sequence or mapping interfaces, and callables might be coded for use with an API that expects a function. You generally shouldn’t implement expression operators if they don’t naturally map to your objects naturally and logically, though—use normally named methods instead.
A5 [中文] 当它让接口更自然且与对象含义一致时；不要为重载而重载。

========================================================================
第 31 章  Designing with Classes
         用类做设计
归属：Part VI 类与 OOP Classes and OOP
级别：必读·设计    优先级：中高
========================================================================

【我的理解与总结】
多重继承、组合、委托、绑定方法、伪私有、MRO。

【对你的帮助】
设计层面选型。

【建议掌握程度】
掌握约 80%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What is multiple inheritance?
Q1 [中文] 什么是多重继承？
A1 [EN] Multiple inheritance occurs when a class inherits from more than one superclass; it’s useful for mixing together multiple packages of class- based code. The left-to-right order in class statement headers determines the general order of attribute searches, and the MRO specializes search for diamonds with common superclasses.
A1 [中文] 一个类继承多个超类，混合多套行为；搜索顺序与 MRO 有关。

── 第 2 题 ──
Q2 [EN] What is composition?
Q2 [中文] 什么是组合？
A2 [EN] Composition is a technique whereby a controller class embeds and directs a number of objects and provides an interface all its own; it’s a way to build up larger structures with classes.
A2 [中文] 控制器类嵌入并调度其他对象（has-a）。

── 第 3 题 ──
Q3 [EN] What is delegation?
Q3 [中文] 什么是委托？
A3 [EN] Delegation involves wrapping an object in a proxy class, which adds extra behavior and passes other operations to the wrapped object. The proxy generally retains the interface of the wrapped object.
A3 [中文] 包装对象，用 __getattr__ 等把请求转给内部对象。

── 第 4 题 ──
Q4 [EN] What are bound methods?
Q4 [中文] 什么是绑定方法？
A4 [EN] Bound methods combine an instance and a method function; you can call them without passing in an instance object explicitly because the original instance is still available in the instance+function pair.
A4 [中文] 通过实例取出的方法，已绑定 self，可直接调用。

── 第 5 题 ──
Q5 [EN] What are pseudoprivate attributes used for?
Q5 [中文] 伪私有属性用途？
A5 [EN] Pseudoprivate attributes, whose names begin but do not end with two leading underscores (e.g., __ X), are used to localize names to the enclosing class. This includes both class attributes, like methods defined inside the class statement, and self instance attributes assigned inside the class’s methods. Such names are expanded to include the class name, which makes them generally unique among all classes in an inheritance tree.
A5 [中文] __name 改写为 _Class__name，减少子类意外覆盖。

── 第 6 题 ──
Q6 [EN] How does the MRO inheritance search order differ from DFLR?
Q6 [中文] MRO 与 DFLR 差别？
A6 [EN] The MRO selects same-named attributes in a lower subclass over those in a higher common superclass in multiple-inheritance “diamond” trees —effectively searching across before up in this specific case. The DFLR is otherwise the same; in fact, the MRO is defined by starting with the DFLR order and then removing all but the last (rightmost) appearances of classes that are visited more than once. This differs from DFLR only when there are duplicates, which arise only in diamonds that have common superclasses. That said, the built-in object makes every multiple-inheritance tree a diamond, so this is a common, if implicit, occurrence.
A6 [中文] 钻石继承时 MRO（C3）比简单深度优先左到右更合理，避免重复并保持单调。

========================================================================
第 32 章  Advanced Class Topics
         类的高级主题
归属：Part VI 类与 OOP Classes and OOP
级别：选读·进阶    优先级：中
========================================================================

【我的理解与总结】
扩展内置类型、装饰器用途、静态方法、slots/super 是否该用。

【对你的帮助】
高级类工具地图。

【建议掌握程度】
掌握约 70%：知道工具，用时再查。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] Name two ways to extend a built-in object type.
Q1 [中文] 扩展内置类型的两种方式？
A1 [EN] You can embed a built-in object in a wrapper class, or subclass the built- in type directly. The latter approach tends to be simpler, as most original behavior is automatically inherited. This works because types are classes, though the way you create an instance from a type/class determines its functionality.
A1 [中文] 包装嵌入，或直接子类化内置类型（通常更简单）。

── 第 2 题 ──
Q2 [EN] What are function and class decorators used for?
Q2 [中文] 函数与类装饰器用途？
A2 [EN] Function decorators are generally used to manage a function or method or add to it a layer of logic that is run each time the function or method is called. They can be used to log or count calls to a function, check its argument types, and so on. They are also used to “declare” static methods (simple functions in a class that are not passed an instance, however they are called), as well as class methods and properties. Class decorators are similar but manage whole objects and their interfaces instead of a function call.
A2 [中文] 管理/增强函数或类：在 def/class 结束时自动插入包装逻辑。

── 第 3 题 ──
Q3 [EN] How are normal and static methods different?
Q3 [中文] 普通方法与静态方法差别？
A3 [EN] Normal (instance) methods receive a self argument (the implied instance), but static methods do not. Static methods are simple functions nested in class objects. To make a method static, it must either be run through a special built-in function or be decorated with decorator syntax. Python also allows simple functions in a class to be called through the class without this step, but calls through instances still require static-method declaration.
A3 [中文] 普通方法自动接实例；静态方法无自动 self，像巢在类中的函数。

── 第 4 题 ──
Q4 [EN] Are tools like __slots__ and super valid to use in your code?
Q4 [中文] __slots__ 与 super 能用吗？
A4 [EN] Of course, but you shouldn’t use advanced tools automatically without carefully considering their implications. Slots, for example, can break code; super can mask later problems when used for single inheritance, and in multiple inheritance brings with it substantial complexity for an isolated use case; and both require universal deployment to be most useful. Evaluating new or advanced tools is a primary task of any engineer, and this is why we explored trade-offs in this chapter. This book’s goal is not to tell you which tools to use but to underscore the importance of objectively analyzing them—a task often given too low a priority in the software field. In engineering, as in life in general, we shouldn’t let other people make choices for us.
A4 [中文] 能，但是可选工具：slots 省内存/限属性；super 协作多重继承。用前需理解行为。

========================================================================
第 33 章  Exception Basics
         异常基础
归属：Part VII 异常与工具 Exceptions and Tools
级别：必读·核心    优先级：极高
========================================================================

【我的理解与总结】
异常用途、未处理后果、恢复、触发、终止动作。

【对你的帮助】
稳健程序必备。

【建议掌握程度】
掌握约 95%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] Name three things that exception processing is good for.
Q1 [中文] 异常处理的三件好处？
A1 [EN] Exception processing is useful for error handling, termination actions, and event notification. It can also simplify the handling of special cases and can be used to implement alternative control flows as a kind of structured “go-to” operation. In general, exception processing also cuts down on the amount of error-checking code your program may require —because all errors filter up to handlers, you may not need to test the outcome of every operation (see this chapter’s sidebar “
A1 [中文] 错误处理、终止动作、事件通知；也可简化特例、作结构化控制流；减少遍地错误检查。

── 第 2 题 ──
Q2 [EN] What happens to an exception if you don’t do anything special to handle it?
Q2 [中文] 不处理异常会怎样？
A2 [EN] Any uncaught exception eventually filters up to the default exception handler Python provides at the top of your program. This handler prints the familiar error message and shuts down your program.
A2 [中文] 向上传播至顶层，默认打印回溯并结束程序（线程等场景规则类似）。

── 第 3 题 ──
Q3 [EN] How can your script recover from an exception?
Q3 [中文] 脚本如何从异常恢复？
A3 [EN] If you don’t want the default message and shutdown, you can code try statements with except clauses to catch and recover from exceptions that are raised within its nested code block. Once an exception is caught, the exception is terminated and your program continues after the try.
A3 [中文] try/except 捕获后继续运行。

── 第 4 题 ──
Q4 [EN] Name two ways to trigger exceptions in your script.
Q4 [中文] 两种触发异常的方式？
A4 [EN] The raise and assert statements can be used to trigger an exception, exactly as if it had been raised by Python itself. In principle, you can also raise an exception by making a programming mistake, but that’s not usually an explicit goal!
A4 [中文] raise；Python/库自动引发；assert 失败等。

── 第 5 题 ──
Q5 [EN] Name two ways to specify actions to be run at termination time, whether an exception occurs or not.
Q5 [中文] 两种无论是否异常都在终止时运行的动作？
A5 [EN] The try statement with a finally clause can be used to ensure actions are run after a block of code exits, regardless of whether the block raises an exception or not. The with statement can also be used to ensure termination actions are run, but only when processing object types that support it.
A5 [中文] try/finally；with 上下文管理器的退出过程。

========================================================================
第 34 章  Exception Coding Details
         异常编码细节
归属：Part VII 异常与工具 Exceptions and Tools
级别：必读·核心    优先级：高
========================================================================

【我的理解与总结】
try/raise/assert/with 各自职责。

【对你的帮助】
异常语句全家桶。

【建议掌握程度】
掌握约 90%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What is the try statement for?
Q1 [中文] try 语句做什么？
A1 [EN] The try statement catches and recovers from exceptions—it specifies a block of code to run and one or more handlers for exceptions that may be raised during the block’s execution.
A1 [中文] 捕获并恢复异常：指定代码块与处理程序。

── 第 2 题 ──
Q2 [EN] What are the two common variations of the try statement?
Q2 [中文] try 两种常见变体？
A2 [EN] The two common variations on the try statement are try / except / else (for catching exceptions) and try / finally (for specifying cleanup actions that must occur whether an exception is raised or not). Despite these logically distinct roles, the except and finally blocks may be mixed in the same statement, so the two forms are really part of the single try statement. Even when mixed with except, though, the finally is still run on the way out of the try, regardless of what exceptions may have been raised or handled. In fact, the combined form is equivalent to nesting a try / except / else in a try / finally.
A2 [中文] try/except/else 捕获；try/finally 清理。也可合并。

── 第 3 题 ──
Q3 [EN] What is the raise statement for?
Q3 [中文] raise 做什么？
A3 [EN] The raise statement raises (triggers) an exception. Python raises built- in exceptions on errors internally, but your scripts can trigger built-in or user-defined exceptions too with raise.
A3 [中文] 主动抛出异常（或重新抛出）。

── 第 4 题 ──
Q4 [EN] What is the assert statement designed to do, and what other statement is it like?
Q4 [中文] assert 设计目的？像哪个语句？
A4 [EN] The assert statement raises an AssertionError exception if a condition is false. It’s similar to a conditional raise statement wrapped up in an if statement, and can be disabled with a –O command switch.
A4 [中文] 调试期断言条件为真，否则 raiser AssertionError；像有条件的 raise。

── 第 5 题 ──
Q5 [EN] What is the with statement designed to do, and what other statement is it like?
Q5 [中文] with 设计目的？像哪个语句？
A5 [EN] The with statement is designed to automate startup and termination activities that must occur around a block of code. It is roughly like a try / finally combination in that its exit actions run whether an exception occurred or not, but it employs an object-based protocol for specifying entry and exit actions and may reduce code size for context- manger users. Still, it’s not quite as general, as it applies only to objects that support its protocol; try with finally clauses can handle more use cases.
A5 [中文] 包装支持上下文协议的对象，保证获取/释放；常类似“必执行清理”的 finally 模式。

========================================================================
第 35 章  Exception Objects
         异常对象
归属：Part VII 异常与工具 Exceptions and Tools
级别：必读·核心    优先级：高
========================================================================

【我的理解与总结】
自定义异常约束、匹配规则、上下文与消息、except*。

【对你的帮助】
异常也是类层次。

【建议掌握程度】
掌握约 85%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What are the two main constraints on user-defined exceptions in Python?
Q1 [中文] 用户定义异常的两个主要约束？
A1 [EN] Exceptions must be defined by classes (that is, a class instance object is raised and caught). In addition, exception classes must be derived from the built-in class BaseException or one of its subclasses; most programs inherit from its Exception subclass to support catchall handlers for normal kinds of exceptions.
A1 [中文] 必须是类（抛实例）；且继承 BaseException（通常继承 Exception）。

── 第 2 题 ──
Q2 [EN] How are raised exceptions matched to except handler clauses?
Q2 [中文] raise 的异常如何匹配 except？
A2 [EN] Exceptions match by superclass relationships: naming a superclass in an exception handler will catch instances of that class, as well as instances of any of its subclasses lower in the class tree. Because of this, you can think of superclasses as general exception categories and subclasses as more specific types of exceptions within those categories.
A2 [中文] 按类继承关系：except 写超类可抓住子类实例。

── 第 3 题 ──
Q3 [EN] Name two ways that you can attach context information to exception objects.
Q3 [中文] 两种给异常对象附加上下文的方式？
A3 [EN] You can attach context information to exceptions with either custom or default constructors. A custom constructor can fill out attributes in a raised instance object that are specific to the program. For simpler needs, built-in exception superclasses provide a default constructor that stores its arguments on the instance automatically as tuple attribute args. Handlers can list a variable to be assigned to the raised instance, then go through this name to access attached state information and call any methods defined in the class.
A3 [中文] 构造传参；raise X from Y；自定义属性等。

── 第 4 题 ──
Q4 [EN] Name two ways that you can specify the error-message text for exception objects.
Q4 [中文] 两种指定异常错误消息文本的方式？
A4 [EN] The error-message text in exceptions can be specified with a custom __str__ operator-overloading method. For simpler needs, built-in exception superclasses automatically display anything you pass to the class constructor. Operations like print and str automatically fetch the display string of an exception object when it is printed either explicitly or as part of an error message.
A4 [中文] 构造时传 message；定义 __str__ 等。

── 第 5 题 ──
Q5 [EN] What do except* clauses do in a try statement?
Q5 [中文] except* 做什么？
A5 [EN] In a try, except* is used to run possibly multiple handlers for exceptions raised as part of a group. The except* also comes with heavy semantics, has special-case syntax and rules, does not combine with basic except, and is rarely useful in most Python programs. Nevertheless, there it is.
A5 [中文] 处理异常组（ExceptionGroup）中的子异常（3.11+ 相关特性）。

========================================================================
第 36 章  Exception Design Tips and Gotchas
         异常设计建议与陷阱
归属：Part VII 异常与工具 Exceptions and Tools
级别：选读·轻松    优先级：低
========================================================================

【我的理解与总结】
封面老鼠彩蛋题 + 设计态度。

【对你的帮助】
轻松收尾 Part VII；设计原则见正文。

【建议掌握程度】
浏览即可；原则掌握约 80%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What’s up with the mouse on the cover of this book?
Q1 [中文] 这本书封面的老鼠是怎么回事？
A1 [EN] OK, this was never mentioned and is hardly a fair question, but for the record: the mouse—really, a wood rat, Neotoma muridae —was chosen for this book’s first edition by its publishing company in the 1990s, based on the fact that this animal is common food for a python. The idea was that the wood rat must learn about the python to avoid being eaten by it. Clever, to be sure, but this also came with a subtler tie-in about Neotoma being pack rats attracted to shiny objects that compulsively collect whatever they come across, which seems an apt metaphor for Python’s history of language-feature accumulation. So enjoy the shiny objects, but don’t get eaten by the constricting reptiles along the way.
A1 [中文] 出版方早期选的木鼠：作为蟒蛇（python）常见食物，寓意“要了解 python 以免被吃”。轻松题，非技术考点。

========================================================================
第 37 章  Unicode and Byte Strings
         Unicode 与字节串
归属：Part VIII 高级主题 Advanced Topics
级别：必读·核心    优先级：高
========================================================================

【我的理解与总结】
str/bytes/bytearray、非 ASCII、文本/二进制文件、编码、BOM。

【对你的帮助】
根治乱码。

【建议掌握程度】
掌握约 90%。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What are the names and roles of string object types in Python?
Q1 [中文] 字符串对象类型名称与角色？
A1 [EN] Python has three string types: str (for Unicode text, including ASCII), bytes (for binary data with absolute byte values), and bytearray (a mutable flavor of bytes). The str type usually represents content stored in text files, and the other two types generally represent content stored in binary files (including encoded text).
A1 [中文] str 文本 Unicode；bytes 二进制字节；bytearray 可变字节。

── 第 2 题 ──
Q2 [EN] How do Python’s string types differ in terms of operations?
Q2 [中文] 各字符串类型在操作上差别？
A2 [EN] Python’s string types share almost all the same operations: method calls, sequence operations, and even larger tools like pattern matching work the same way. On the other hand, only str supports string formatting’s format method and f'…' f-strings, and bytearray has an additional set of operations that perform in-place changes. The str and bytes types also have methods for encoding and decoding text, respectively.
A2 [中文] 大多操作相似；但 str 与 bytes 不能混加；编码解码在边界；bytearray 可变。

── 第 3 题 ──
Q3 [EN] How can you code non-ASCII Unicode characters in a string in Python?
Q3 [中文] 如何在字符串中写非 ASCII 的 Unicode？
A3 [EN] Non-ASCII Unicode characters can be coded in a str string with both hex (\xNN) and Unicode (\uNNNN, \UNNNNNNNN) escapes for code points, both of which denote character code points. They can also be coded in their encoded form as bytes using hex escapes and decoded to text. On most devices, non-ASCII characters—accented characters and emojis, for example—can also be typed or pasted directly into code and are interpreted per the UTF-8 default or an encoding-directive comment at the top of a source code file.
A3 [中文] 源文件编码声明/默认 UTF-8、\u/\U 转义、直接写字符等。

── 第 4 题 ──
Q4 [EN] What are the main differences between text- and binary-mode files in Python?
Q4 [中文] 文本模式与二进制模式文件主要差别？
A4 [EN] Text-mode files assume their content is Unicode text (even if it’s all ASCII) and automatically decode when reading and encode when writing. With binary-mode files, bytes are transferred to and from the file unchanged. The contents of text-mode files are usually represented as str objects in your script, and the contents of binary files are represented as bytes (or bytearray) objects. Text-mode files also handle BOMs for certain encoding types and automatically translate newline sequences to and from the single \n character on input and output unless this is explicitly disabled; binary-mode files do not perform either of these steps.
A4 [中文] 文本：编解码、换行转换；二进制：原始字节，模式含 b。

── 第 5 题 ──
Q5 [EN] How would you read a Unicode text file that contains text in a different encoding than the default for your platform?
Q5 [中文] 如何读与平台默认编码不同的 Unicode 文本文件？
A5 [EN] To read files encoded in a different encoding than the default for your platform, simply pass the name of the file’s encoding to the open built- in function; data will be decoded per the specified encoding when it is read from the file, and you’ll get back a decoded Unicode-text str string that has no encoding. You can also read in binary mode and manually decode the bytes to a string by giving an encoding name, but this involves extra work and is somewhat error-prone for multibyte characters (you may accidentally read a partial character sequence when loading content by bytes or chunks).
A5 [中文] open(path, encoding='该编码')。

── 第 6 题 ──
Q6 [EN] How can you create a Unicode text file in a specific encoding format?
Q6 [中文] 如何按特定编码创建 Unicode 文本文件？
A6 [EN] To create a Unicode text file in a specific encoding format, pass the desired encoding name to open; strings will be encoded per the desired encoding when they are written to the file. You can also manually encode a string to bytes and write it in binary mode, but this is usually extra work.
A6 [中文] open(..., 'w', encoding='...') 写入 str。

── 第 7 题 ──
Q7 [EN] Why is ASCII text considered to be a kind of Unicode text?
Q7 [中文] 为何 ASCII 也是一种 Unicode 文本？
A7 [EN] ASCII text is considered to be a kind of Unicode text because its 7-bit (0..127) range of values is a subset of many Unicode encodings. For example, valid ASCII text is also valid Latin-1 text (Latin-1 simply assigns the remaining possible values in an 8-bit byte to additional characters) and valid UTF-8 text (UTF-8 uses a variable-byte scheme for representing more characters, but ASCII characters are still represented with the same values in a single byte). This makes Unicode backward compatible with ASCII, as long as the encodings used represent ASCII the same way.
A7 [中文] ASCII 是 Unicode 码点的子集；UTF-8 等对 ASCII 兼容。

── 第 8 题 ──
Q8 [EN] What do BOM and normalization mean in Unicode?
Q8 [中文] BOM 与规范化在 Unicode 中是什么？
A8 [EN] The Unicode BOM is a sequence of bytes added to the front of a text string or file in some encodings to both identify the encoding and give the string’s endianness. Unicode normalization converts characters to a common format to neutralize differences that arise for characters that have multiple code-point values in the Unicode standard and would fail to match by simple code-point (character) equality.
A8 [中文] BOM 字节序/编码标记；规范化是同一字符不同码点序列的标准等价形式。

── 第 9 题 ──
Q9 [EN] How large an impact does Python’s text/binary string dichotomy have on your code?
Q9 [中文] 文本/二进制二分对代码影响有多大？
A9 [EN] The impact of Python’s strings model depends upon the types of strings you use. For scripts that use simple ASCII text on platforms with ASCII-compatible default encodings, the impact is probably minor: the str string sans encodings suffices in this case. Moreover, although string-related tools in the standard library, such as re, struct, pickle, and os, may technically use different types in different contexts, the effect is largely irrelevant to most programs because Python’s str and bytes support almost identical interfaces. On the other hand, if you process non-ASCII Unicode text, you’ll need to use str and pass encodings to open; if you deal with binary data files, you’ll need to deal with content as bytes objects; and if your code must work across many platforms or process content from arbitrary sources, you’ll want to use explicit encodings for text files. In general, Unicode is the way the text world works today and will be a must-know tool for most Python users. 1 File fine points: most of this chapter’s file examples use filenames relative to, and hence stored in, the current directory, so be sure to run them in a directory (a.k.a. folder) where you have permission to create files; cd to one in your shell or IDE if needed. This may matter on platforms with major storage constraints like Android, but you’ll probably already be in a safe folder in most apps. Also, file extensions (e.g.,.txt) don’t mean anything to Python and are technically optional; some IDEs that hang on to objects for debugging may require manual close calls to flush changes too; and purists take note that file is no longer a built-in name in Python and OK to use as a variable here!
A9 [中文] 边界（I/O、网络、C 扩展）必须分清；纯文本处理多在 str 上，影响可控但不可忽视。

========================================================================
第 38 章  Managed Attributes
         托管属性
归属：Part VIII 高级主题 Advanced Topics
级别：选读·高级    优先级：中低
========================================================================

【我的理解与总结】
__getattr__ vs __getattribute__、property vs 描述符、与装饰器关系。

【对你的帮助】
框架/ORM 向；日常先会 property。

【建议掌握程度】
掌握约 50–70% 按目标。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] How do __getattr__ and __getattribute__ differ?
Q1 [中文] __getattr__ 与 __getattribute__ 差别？
A1 [EN] The __getattr__ method is run for explicit fetches of undefined attributes only (i.e., those not present on an instance and not inherited from any of its classes). By contrast, the __getattribute__ method is called for every explicit attribute fetch, whether the attribute is defined or not. Because of this, code inside a __getattr__ can freely fetch other attributes if they are defined, whereas __getattribute__ must use special code for all such attribute fetches to avoid looping or extra calls (it must route fetches to a superclass to skip itself). Neither method is run for implicit fetches of built-in operations (sans the next chapter’s heroics).
A1 [中文] __getattr__ 仅在正常查找失败（未定义属性）时调用；__getattribute__ 每次属性获取都调用，内必须谨慎避免递归。

── 第 2 题 ──
Q2 [EN] How do properties and descriptors differ?
Q2 [中文] property 与描述符差别？
A2 [EN] Properties serve a specific role, whereas descriptors are more general. Properties define get, set, and delete functions for a specific attribute; descriptors provide a class with methods for these actions, too, but they provide extra flexibility to support more arbitrary actions. In fact, properties are really a simple way to create a specific kind of descriptor —one that runs functions on attribute accesses. Coding differs too: a property is created with a built-in function, and a descriptor is coded with a class; thus, descriptors can leverage all the usual OOP features of classes, such as inheritance. Moreover, in addition to the instance’s state information, descriptors have local state of their own, which can sometimes avoid name collisions in the instance.
A2 [中文] property 是描述符的便捷特例；描述符是实现 __get__/__set__ 等的类，更通用。

── 第 3 题 ──
Q3 [EN] How are properties and decorators related?
Q3 [中文] property 与装饰器关系？
A3 [EN] Properties can be coded with decorator syntax. Because the property built-in accepts a single function argument and returns a function, it can be used directly as a function decorator to define a fetch-access property. Due to the name rebinding behavior of decorators, the name of the decorated function is assigned to a property whose get accessor is set to the original function decorated ( ```python name=property(name) ``` ). Property setter and deleter attributes allow us to further add set and delete accessors with decoration syntax—they set the accessor to the decorated function and return the augmented property. Some may find this a bit clumsy, but this is subjective.
A3 [中文] @property 等用装饰器语法把方法变成托管属性。

── 第 4 题 ──
Q4 [EN] What are the main functional differences between __getattr__ and __getattribute__ and properties and descriptors?
Q4 [中文] __getattr__/__getattribute__ 与 property/描述符主要功能差别？
A4 [EN] The __getattr__ and __getattribute__ methods are more generic: they can be used to catch arbitrarily many attributes. In contrast, each property or descriptor provides access interception for only one specific attribute—we can’t catch every attribute fetch with a single property or descriptor. On the other hand, properties and descriptors handle both attribute fetch and assignment by design: __getattr__ and __getattribute__ handle fetches only; to intercept assignments as well, __setattr__ must also be coded. The implementation is also different: __getattr__ and __getattribute__ are operatoroverloading methods, whereas properties and descriptors are objects manually assigned to class attributes. Unlike the others, properties and descriptors can also sometimes avoid extra calls on assignment to unmanaged names and show up in dir results automatically, but are also narrower in scope—they can’t address generic delegation goals. In Python evolution, new features tend to offer alternatives but often do not fully subsume what came before.
A4 [中文] 前两者在实例上拦截（更宽、偏动态）；property/描述符在类上按属性定义拦截（更精确、常更高效可计算）。

========================================================================
第 39 章  Decorators
         装饰器
归属：Part VIII 高级主题 Advanced Topics
级别：选读·高级    优先级：中高
========================================================================

【我的理解与总结】
改造计时装饰器支持方法、类装饰器性能开关等（编码题）。

【对你的帮助】
读库与横切逻辑；完整答案在书 examples 的 _QuizAnswers。

【建议掌握程度】
掌握约 75%：会写基础装饰器；编码题可对照官方答案。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] Method decorators : As mentioned in one of this chapter’s notes, the timerdeco2.py module’s call-timer decorator that we wrote in Example 39-10 of “Adding Decorator Arguments” can be applied only to simple functions because it uses a nested class with a __call__ operator-overloading method to catch calls. This structure does not work for a class’s methods because the decorator instance is passed to self, not the subject-class instance. Rewrite this decorator so that it can be applied to both simple functions and methods in classes, and test it on both functions and methods. (Hint: see “Class Pitfall: Decorating Methods” for pointers.) Note that you will probably need to use function-object attributes to keep track of total time, since you won’t have a nested class for state retention and can’t access nonlocals from outside the decorator code.
Q1 [中文] 方法装饰器：改写计时装饰器使其同时适用于普通函数与类方法，并测试。
A1 [EN] Coding solutions are in this chapter's _QuizAnswers subfolder of the book examples package (not printed in full in the chapter text).
A1 [中文] 编码题。要点：不要用仅适函数的嵌套类 __call__ 抢 self；用函数包装器+函数属性存总耗时。完整答案见书 examples 的 _QuizAnswers。

── 第 2 题 ──
Q2 [EN] Class decorators : The Public / Private class decorators we wrote in module access2.py of Example 39-18 in this chapter’s first case study example will add performance costs to every attribute fetch in a decorated class. Although we could simply delete the @ decoration line to gain speed, we could also augment the decorator itself to check the __debug__ switch and perform no wrapping at all when the –O Python flag is passed on the command line—just as we did for the argument range-test decorators. That way, we can speed our program without changing its source via command-line arguments (python –O main.py). While we’re at it, we could also use one of the mix-in superclass techniques we studied to catch a few built-in operations too. Code and test these two extensions.
Q2 [中文] 类装饰器：为 Public/Private 类装饰器增加开关以免处处付属性拦截成本。
A2 [EN] Coding solutions are in this chapter's _QuizAnswers subfolder of the book examples package (not printed in full in the chapter text).
A2 [中文] 编码题。可增加启用/禁用参数或模式，在装饰器内短接拦截逻辑。详见 _QuizAnswers。

── 第 3 题 ──
Q3 [EN] Generalized argument validations : The function and method decorator we wrote in rangetest.py of Example 39-25 checks that passed arguments are in a valid range, but the same code pattern could apply to similar goals such as argument type testing and possibly more. Generalize the range tester so that its single code base can be used for multiple kinds of argument validations. Passed-in validation functions may be the simplest solution given the coding structure here, though subclasses that provide expected methods can often provide similar generalization routes as well. This is substantially challenging, so be sure to see the solution for tips.
Q3 [中文] （若有第3题）按教材要求修改/扩展装饰器示例。
A3 [EN] Coding solutions are in this chapter's _QuizAnswers subfolder of the book examples package (not printed in full in the chapter text).
A3 [中文] 同样以 _QuizAnswers 目录中代码与 _Notes.txt 为准。

========================================================================
第 40 章  Metaclasses
         元类
归属：Part VIII 高级主题 Advanced Topics
级别：选读·专家    优先级：低
========================================================================

【我的理解与总结】
元类定义、声明方式、与类装饰器重叠。

【对你的帮助】
理解框架底层；多数业务不必手写。

【建议掌握程度】
掌握约 40–60% 了解级。

【章节测验 · 中英对照】

── 第 1 题 ──
Q1 [EN] What is a metaclass?
Q1 [中文] 什么是元类？
A1 [EN] A metaclass is a class used to create a class. Classes are instances of the type class by default. Metaclasses are usually subclasses of the type class, which customize classes. They may redefine class-creation protocol methods like __new__ and __init__ to customize the class creation call issued at the end of a class statement. They may also define data and methods that provide behavior for their instance classes, but these are inherited only by classes, not their nonclass instances. Metaclasses can also be coded in other ways—as simple functions, for example—but they are responsible for making and returning an object for the new class. Finally, metaclasses constitute a secondary pathway for inheritance search used for classes alone; this allows classes to ape normal instance behavior, though it bifurcates the class story for relatively rare and narrow roles.
A1 [中文] 用来创建类的类。默认类是 type 的实例；元类常为 type 子类，定制类创建。

── 第 2 题 ──
Q2 [EN] How do you declare the metaclass of a class?
Q2 [中文] 如何声明类的元类？
A2 [EN] Use a keyword argument in the class header line: class C (metaclass= M ```python ) ``` . The class header line can also name normal superclasses before the metaclass keyword argument; these superclasses are searched before metaclasses for inheritance run from a class.
A2 [中文] class C(metaclass=Meta): ...（或教材所述其它形式/继承牵连）。

── 第 3 题 ──
Q3 [EN] How do class decorators overlap with metaclasses for managing classes?
Q3 [中文] 类装饰器与元类在“管理类”上如何重叠？
A3 [EN] Because both are automatically triggered at the end of a class statement, class decorators and metaclasses can both be used to manage classes. Decorators rebind a class name to a callable’s result, and metaclasses route class creation through a callable, but both hooks can be used for similar purposes. To manage classes, decorators simply augment and return the original class objects. Metaclasses augment a class after or before they create it. As noted, metaclasses can also provide behavior for their instance classes in the form of methods that are not immediately supported by decorators, though the objects returned by decorators can do anything supported by the Python language.
A3 [中文] 二者都能在类创建后介入并修改类。装饰器更简单常见；元类更深（构建过程、继承体系）。许多任务二者皆可，优先简单者。

========================================================================
第 41 章  All Good Things
         美好的一切（收尾）
归属：收尾 Epilogue
级别：选读·收尾    优先级：低
========================================================================

【我的理解与总结】
全书收尾随笔，无测验。

【对你的帮助】
反思语言演进。

【建议掌握程度】
浏览即可。

【章节测验】本章无 Test Your Knowledge 测验（或无需测验）。

========================================================================
全书章节测验整理完毕。把错题章号记下来，隔天用中文复述答案。
文件位置：C:\Users\Administrator\Desktop\Python-learning-main\leaning python 6th books\txt\测验以及答案.txt
========================================================================
